# 20260812-dumpling PRD / 开发变更日志

## 2026-08-12 - 初始化 PRD-first 可审计 AI 研发闭环

### 编写人
- dumpling

### 请求
- 为 `bwatch` 初始化通用 PRD-first 可审计 AI 研发闭环。

### 影响模块
- AI 入口文件。
- 项目内 PRD Keeper Skill。
- 分层 PRD 文档。
- PRD 日志与校验脚本。

### 代码变更
- 新增或更新 `AGENTS.md`、`CODEX.md`、`CLAUDE.md` 薄入口。
- 新增或更新 `.agent/skills/prd-keeper/` 项目内治理 Skill。
- 新增或更新 `docs/prd/` 分层 PRD 模板。

### PRD 更新
- 创建初始 PRD 总入口、产品概览、架构说明、模块索引和 PRD Keeper 模块文档。

### 验证
- 初始化后需运行 `.agent/skills/prd-keeper/scripts/check_prd_keeper.py`。

### 风险/后续事项
- 模板中的 `待确认` 项需要结合真实代码、配置和用户说明继续填充。

## 2026-08-12 - 实现 B站直播监控 VSCode 扩展

### 编写人
- dumpling

### 请求
- 实现一个 VSCode 插件，以侧边栏形式监控多个 B站直播间的开播状态、在线观众人数和已开播时长。
- 支持通过侧栏和 VSCode 设置维护房间号。
- 自动刷新默认开启，可开关，可修改间隔且不得低于 15 秒。
- 支持可开关的开播提醒。

### 影响模块
- 直播监控扩展。
- VSCode 扩展配置、命令和 Webview UI。
- PRD 总入口、产品概览、架构说明和模块索引。

### 代码变更
- 新增 TypeScript VSCode 扩展工程骨架：`package.json`、`tsconfig.json`、`.gitignore`、`.vscodeignore`。
- 新增扩展入口、Webview Provider、直播监控服务、B站接口 client、配置和时间工具。
- 新增 Webview 样式、脚本和 Activity Bar 图标。
- 新增 Node 单元测试，覆盖配置校验、接口标准化、部分房间失败和开播提醒去重。

### PRD 更新
- 回填 `bwatch` 当前产品定位、核心场景、配置项、数据流和第三方服务边界。
- 新增 `docs/prd/modules/live-monitor.md` 描述直播监控模块当前行为。
- 更新 `docs/prd/modules/README.md` 链接直播监控模块。

### 验证
- 待运行：`npm install`。
- 待运行：`npm test`。
- 待运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`。

### 风险/后续事项
- B站直播接口不是本项目控制的正式稳定 API，后续可能需要根据接口变化调整适配。
- 尚未完成真实 Extension Development Host 手测。

## 2026-08-12 - 修复 B站直播接口路由异常

### 编写人
- dumpling

### 请求
- 用户反馈侧边栏房间状态显示 `route_exception`，无法获取直播间数据。

### 影响模块
- 直播监控扩展。
- B站接口 client。

### 代码变更
- 将接口从 `room/v1/Room/get_info_by_id?ids=...` 调整为 `xlive/web-room/v1/index/getRoomBaseInfo`。
- 批量查询改为 `req_biz=web_room_componet` 加重复 `room_ids` 参数。
- 更新测试中的接口响应字段示例。

### PRD 更新
- 更新架构文档和直播监控模块文档中的 B站接口契约说明。

### 验证
- 已用用户截图中的房间号 `1727074031`、`26795` 通过 `curl.exe` 验证新接口返回 `code: 0`。
- 已运行 `npm test`，7 个测试全部通过。

## 2026-08-12 - 修正在线观众字段来源

### 编写人
- dumpling

### 请求
- 用户反馈侧边栏显示的在线观众为 7 万多，但直播间页面实际为 900 多人。

### 影响模块
- 直播监控扩展。
- B站接口 client。
- Webview 指标展示。

### 代码变更
- 将房间基础信息接口中的 `online` 保留为 `popularity` 人气/热度字段。
- 对直播中房间追加调用 `xlive/general-interface/v1/rank/getOnlineGoldRank`，使用 `onlineNum` 作为在线观众人数。
- Webview 将第三个指标从“短号”调整为“人气”。
- 增加单元测试覆盖在线榜接口覆盖基础接口人气值的场景。

### PRD 更新
- 更新产品概览、架构文档和直播监控模块文档中的在线观众/人气字段边界。

### 验证
- 已用 `curl.exe` 验证房间 `1727074031` 的在线榜接口返回 `onlineNum: 969`，与直播页 900 多人一致。
- 已运行 `npm test`，8 个测试全部通过。

## 2026-08-12 - 在侧边栏新增开播提醒开关

### 编写人
- dumpling

### 请求
- 用户要求在列表页上方增加开关按钮，用于开启或关闭开播提醒。

### 影响模块
- 直播监控扩展。
- Webview 控制区。
- VSCode 配置写入。

### 代码变更
- 在侧边栏设置区新增“开播提醒”复选开关。
- Webview 新增 `toggleLiveStartNotifications` 消息。
- 扩展入口新增 `setLiveStartNotificationsEnabled()`，将开关同步到 `bwatch.notifications.liveStart.enabled`。
- 顺手修复扩展入口、Webview Provider 和 Webview 脚本中的中文乱码文案。

### PRD 更新
- 更新直播监控模块文档中的用户交互说明。

### 验证
- 待运行：`npm test`。
- 待运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`。

## 2026-08-12 - 增加主播名模糊搜索添加直播间

### 编写人
- dumpling

### 请求
- 用户要求支持通过模糊搜索主播名字查找直播间并加入监控列表。

### 影响模块
- 直播监控扩展。
- B站接口 client。
- 侧边栏添加入口和命令面板添加入口。

### 代码变更
- 新增 `LiveAnchorSearchResult` 类型。
- `BilibiliLiveClient` 新增 `searchLiveAnchors()`，调用 B站直播主播搜索接口。
- 添加入口改为“房间号或主播名”：纯数字直接添加房间号，非纯数字调用主播搜索。
- 搜索结果通过 VSCode Quick Pick 展示主播名、房间号、分区、关注数和直播状态，选择后写入 `bwatch.rooms`。
- 新增主播搜索单元测试。

### PRD 更新
- 更新产品概览、架构文档和直播监控模块文档中的主播搜索入口、数据流和外部接口说明。

### 验证
- 已用 `curl.exe` 验证 `search_type=live_user&keyword=dumpling` 可返回房间号、主播名、分区和关注数。
- 待运行：`npm test`。
- 待运行：PRD keeper 校验。

## 2026-08-12 - 重构添加直播间为弹窗搜索列表

### 编写人
- dumpling

### 请求
- 用户要求重构添加功能：侧边栏原位置只保留一个按钮，点击后打开类似搜索弹窗的界面；输入后展示所有符合房间 ID 或主播名字的直播间列表。

### 影响模块
- 直播监控扩展。
- Webview 添加入口和搜索弹窗。
- 扩展宿主与 Webview 消息协议。

### 代码变更
- 侧边栏顶部移除常驻输入框，仅保留“+”添加按钮和刷新按钮。
- 新增 Webview 内添加弹窗，包含搜索输入框、状态提示和候选直播间列表。
- 新增 `searchRooms` / `searchResults` 消息协议，由扩展宿主执行 B站接口请求并回传结果，避免 Webview 跨域请求。
- 搜索结果同时包含纯数字房间号候选和主播名模糊搜索候选，点击候选后加入 `bwatch.rooms`。

### PRD 更新
- 更新产品概览、架构文档和直播监控模块文档中的添加入口、搜索弹窗和数据流说明。

### 验证
- 待运行：`npm test`。
- 待运行：PRD keeper 校验。

## 2026-08-12 - 增加在线人数历史缓存与迷你走势图

### 编写人
- dumpling

### 请求
- 用户要求缓存每次刷新获取的在线人数，用于绘制每个主播条目内的连续在线人数走势。
- 侧边栏需要统一“走势”按钮、统一横向时间滑杆、“最新”按钮，以及 `独立` / `统一` 纵轴切换。

### 影响模块
- 直播监控扩展。
- Webview 列表布局和前端绘图。
- B站接口 client 在线人数口径。
- PRD 总入口、产品概览、架构说明、直播监控模块文档和 README。

### 代码变更
- 新增 `OnlineViewerHistoryPoint`、`OnlineViewerHistory` 和 `MonitorSnapshot.onlineHistory`。
- 新增 `OnlineHistoryStore`，通过 VSCode `globalState` 持久化最近 24 小时在线人数采样，支持恢复、裁剪和房间删除清理。
- `LiveMonitor` 刷新成功后按房间写入在线人数历史，并把历史数据随快照推送给 Webview。
- 修正在线人数口径：直播中只使用在线榜接口 `onlineNum`；在线榜失败时 `online = null`，不再回退基础接口 `online`；未开播房间 `online = 0`。
- Webview 新增走势控制区和 SVG 迷你走势图；支持统一 6 小时时间窗口、最近 24 小时内滑动、回到最新窗口、独立/统一纵轴切换和 `null` 断线。
- 新增在线历史仓库单元测试，并补充 LiveMonitor 历史采样和 B站 client 在线口径测试。

### PRD 更新
- 更新直播监控模块文档中的在线历史缓存、24 小时保留、最近 6 小时默认窗口、`null` 断线和纵轴切换规则。
- 更新架构文档中的 `OnlineHistoryStore`、`globalState` 持久化和 Snapshot 数据流。
- 更新产品概览、PRD 总入口和 README，补充走势、缓存、粉丝数和舰队总人数等当前能力。

### 验证
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 走势图依赖持续刷新积累采样；刚启用或重启后若历史不足 2 个有效点，会显示“数据积累中”。
- 本轮只实现每个主播条目内的迷你走势图，不做多主播叠加大图。

## 2026-08-12 - 调整直播状态点并新增简略展示模式

### 编写人
- dumpling

### 请求
- 用户要求将刷新成功状态改为整体状态，不再每条记录显示刷新成功点。
- 用户要求沿用原绿色点设计表示是否直播中：绿色为直播中，红色为不在直播。
- 用户要求保留当前展示为详细模式，并新增一个类似股票列表的简略模式，通过按钮切换。

### 影响模块
- Webview 列表布局。
- Webview 顶部工具栏和汇总状态。
- 直播监控模块 PRD、产品概览和 README。

### 代码变更
- 工具栏新增“简略/详细”展示模式切换按钮，默认保持详细模式。
- 详细模式保留现有卡片内容，但左侧状态点改为直播状态点。
- 新增简略模式，用单行列表展示直播状态点、主播名、在线人数、舰队人数和直播时长。
- 顶部汇总区新增整体刷新状态，区分更新中、未刷新、刷新成功、部分失败和刷新失败。

### PRD 更新
- 更新直播监控模块文档中的展示模式、直播状态点和整体刷新状态规则。
- 更新产品概览和 README 中的展示能力描述。

### 验证
- 已运行：`npm.cmd test`，21 个测试通过。
- 待运行：PRD keeper 校验。
- 待运行：`npx.cmd vsce package`。

## 2026-08-12 - 调整走势图滑杆为最新窗口宽度

### 编写人
- dumpling

### 请求
- 用户要求将走势图横条含义改为横轴时间宽度，而不是在历史缓存中横向拖动时间范围。
- 用户要求窗口最大 6 小时、最小 10 分钟。
- 用户要求所有走势图展示从当前最新时间往前所选时间长度的走势。

### 影响模块
- Webview 走势控制区。
- Webview SVG 走势图时间窗口计算。
- 直播监控模块 PRD、产品概览和 README。

### 代码变更
- 走势图滑杆范围改为 `10` 到 `360` 分钟，步长 10 分钟，默认 360 分钟。
- 移除“最新”按钮；当前窗口右端点始终为最新刷新时间。
- `getTrendWindow()` 改为使用 `end = latestRefreshAt`、`start = end - selectedWindowMinutes`。
- 走势标签改为展示“最近 N 分钟/小时”。

### PRD 更新
- 更新直播监控模块文档中的走势图窗口语义、最小/最大范围和当前窗口计算规则。
- 更新产品概览和 README 中的走势图说明。

### 验证
- 已运行：`npm.cmd test`，21 个测试通过。
- 待运行：PRD keeper 校验。
- 待运行：`npx.cmd vsce package`。

## 2026-08-12 - 增加排序与开播状态筛选

### 编写人
- dumpling

### 请求
- 用户要求支持按是否开播、粉丝数量、在线人数、舰长数量/舰队人数等条件排序，并支持从大到小或从小到大切换。
- 用户要求支持切换展示全部、开播和未开播主播。
- 用户明确排序筛选对详细模式和简略模式同时生效。

### 影响模块
- Webview 控制区。
- Webview 详细模式和简略模式列表渲染。
- 走势图共享纵轴数据范围。
- PRD、产品概览和 README。

### 代码变更
- 新增排序控制区，支持默认顺序、是否开播、在线人数、舰队人数、粉丝人数和直播时长。
- 新增排序方向按钮，支持大到小和小到大。
- 新增状态筛选按钮，支持全部、开播和未开播；未开播包含未知/失败状态。
- 渲染前统一计算 `visibleRooms`，详细模式、简略模式和展开走势图共用同一排序筛选结果。
- 统一纵轴走势图的共享 min/max 改为基于当前可见主播计算。

### PRD 更新
- 更新直播监控模块文档中的排序字段、方向、筛选规则和双展示模式生效范围。
- 更新产品概览和 README 的当前能力描述。

### 验证
- 已运行：`npm.cmd test`，21 个测试通过。
- 待运行：PRD keeper 校验。
- 待运行：`npx.cmd vsce package`。

## 2026-08-12 - 优化指标列对齐与时长格式

### 编写人
- dumpling

### 请求
- 用户反馈详细模式中“1小时32分钟”等中文时长过长，导致指标列不够对齐。
- 用户要求时长尽量缩略，用 `00:00` 形式展示。

### 影响模块
- Webview 详细模式和简略模式列表布局。
- 直播监控模块 PRD 与产品概览。

### 代码变更
- 详细模式和简略模式的直播时长统一改为 `HH:MM` 展示。
- 未开播、未知或缺少开播时间时显示 `00:00`。
- 调整在线、时长、舰队指标列宽和数字排版，使卡片内指标更稳定对齐。

### PRD 更新
- 更新直播监控模块文档中的指标列对齐和直播时长展示规则。
- 更新产品概览中简略模式展示字段说明。

### 验证
- 已运行：`npm.cmd test`，21 个测试通过。
- 待运行：PRD keeper 校验。
- 待运行：`npx.cmd vsce package`。

## 2026-08-12 - 窄宽度下直播间指标同行展示

### 编写人
- dumpling

### 请求
- 用户要求当卡片自适应为个人信息和直播间信息上下排列时，直播间的“在线 / 时长 / 舰队”三项信息仍放在同一行。

### 影响模块
- Webview 列表布局。

### 代码变更
- 调整 `media/webview.css` 中 `max-width: 420px` 响应式布局。
- 保持个人信息在第一行，三项直播间指标在第二行同行显示。
- 取消窄宽度下舰队指标单独换到第三行的规则。

### PRD 更新
- 现有直播监控模块文档已描述紧凑列表和窄宽度响应式布局；本次仅追加日志记录具体布局调整。

### 验证
- 待运行：`npm test`。
- 待运行：PRD keeper 校验。

## 2026-08-12 - 增加主播粉丝数展示

### 编写人
- dumpling

### 请求
- 用户要求在主播个人信息中返回粉丝数。

### 影响模块
- 直播监控扩展。
- B站接口 client。
- Webview 列表布局。
- 添加直播间搜索结果展示。

### 代码变更
- `LiveRoomStatus` 新增 `fansCount` 字段。
- 直播间刷新时根据房间基础信息返回的 `uid` 调用 `x/relation/stat?vmid={uid}`，取 `data.follower` 作为主播粉丝数。
- 粉丝数请求失败时保留房间状态刷新结果，粉丝数显示为缺失。
- Webview 主播信息区域新增“粉丝”展示。
- 主播搜索候选详情中的“关注”文案调整为“粉丝”。

### PRD 更新
- 更新产品概览、架构文档和直播监控模块文档中的展示字段、外部接口和失败边界。
- 修正产品概览中“主播搜索”边界描述，使其与当前添加弹窗搜索能力一致。

### 验证
- 已运行：`npm test`，11 个测试通过。
- 待运行：PRD keeper 校验。

## 2026-08-12 - 增加舰队总人数展示

### 编写人
- dumpling

### 请求
- 用户希望添加舰队数量字段；在确认拆分舰长/提督/总督需要翻页获取后，收窄为只显示舰队总人数。

### 影响模块
- 直播监控扩展。
- B站接口 client。
- Webview 列表布局。

### 代码变更
- 新增 `guardFleet.total` 字段。
- 通过 `xlive/app-room/v2/guardTab/topList` 的 `info.num` 获取舰队总人数。
- 列表卡片新增“舰队”紧凑指标，只展示总人数。
- 不获取分页列表，不展示舰长/提督/总督拆分。

### PRD 更新
- 更新直播监控模块文档中的展示字段和外部接口边界。

### 验证
- 待运行：`npm test`。
- 待运行：PRD keeper 校验。

## 2026-08-12 - 紧凑化直播间列表布局

### 编写人
- dumpling

### 请求
- 用户要求每个主播条目再缩进一点，去掉每个主播独立更新时间。
- 顶部只保留列表统一更新时间。
- 每个主播保留一个状态表示是否刷新成功。
- 去掉人气字段，只保留在线人数和开播时长，并让列表更紧凑。

### 影响模块
- 直播监控扩展。
- Webview 列表布局。

### 代码变更
- 直播间卡片改为紧凑单行布局，并增加左侧刷新状态点。
- 卡片移除人气和单房间更新时间展示。
- 卡片只展示在线观众和已开播时长两个指标。
- 增加窄宽度响应式布局，避免侧边栏变窄后内容挤压。

### PRD 更新
- 更新直播监控模块文档中的当前展示字段和刷新状态规则。

### 验证
- 待运行：`npm test`。
- 待运行：PRD keeper 校验。

## 2026-08-12 - 直播时长秒级展示与本地走秒

### 编写人
- dumpling

### 请求
- 用户要求直播时长从 `00:00` 改为 `00:00:00`，展示秒数。
- 用户要求直播状态仍按定时刷新获取，但直播时长秒数根据开播时间点在本地模拟走秒。

### 影响模块
- Webview 详细模式和简略模式列表渲染。
- 直播监控模块 PRD、架构文档、产品概览和 README。

### 代码变更
- 详细模式和简略模式直播时长统一改为 `HH:MM:SS` 展示。
- Webview 为直播中且存在 `liveStartTime` 的时长节点记录开播时间，并用本地 `setInterval` 每秒刷新 `.duration-value` 文本。
- 未开播、未知或缺少开播时间时显示 `00:00:00`。
- 调整详细模式和简略模式时长列宽，避免 `HH:MM:SS` 挤压相邻字段。

### PRD 更新
- 更新直播监控模块文档中的直播时长格式和本地走秒规则。
- 更新架构文档中的 Webview 本地时长更新数据流。
- 更新产品概览中的简略模式字段说明和直播时长本地更新行为。
- 更新 README 功能列表中的秒级时长说明。

### 验证
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 本地走秒依赖接口返回可信的 `liveStartTime`；若 B站接口缺失或异常，UI 会保守显示 `00:00:00`。
- 本地秒级时长只改变展示，不改变直播状态、在线人数、舰队人数等数据的刷新频率。

## 2026-08-12 - 控制面板默认收起

### 编写人
- dumpling

### 请求
- 用户反馈顶部控制面板占用空间过大，希望以细节状态列表作为主要面板。
- 用户希望只有在需要调整控制项时才展开为当前控制状态。

### 影响模块
- Webview 顶部工具栏和控制面板。
- 直播监控模块 PRD、产品概览和 README。

### 代码变更
- 顶部常驻工具栏只保留添加、手动刷新和控制面板展开/收起按钮。
- 将展示模式、自动刷新/提醒、刷新间隔、排序筛选、走势和纵轴控制归入 `control-panel`。
- 控制面板默认收起，展开状态仅保存在当前 Webview 会话内。
- 收起状态下控制面板不占布局空间，主播列表和汇总状态上移成为主视图。

### PRD 更新
- 更新直播监控模块文档中的顶部常驻区和控制面板默认收起规则。
- 更新产品概览中的核心场景描述。
- 更新 README 功能列表中的默认收起说明。

### 验证
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 本次只调整默认布局；控制面板状态保留能力在后续“保留 Webview 控制展示状态”条目中补齐。

## 2026-08-12 - 保留 Webview 控制展示状态

### 编写人
- dumpling

### 请求
- 用户反馈在 VSCode 中切换到其他面板再回到直播监控面板时，控制面板里的选择会被重置。
- 用户要求控制展示形式不管怎样都不要被清空。

### 影响模块
- Webview 控制面板状态管理。
- Webview View 注册选项。
- 直播监控模块 PRD、架构文档、产品概览和 README。

### 代码变更
- Webview 前端通过 VSCode Webview `getState` / `setState` 保存控制面板展开状态、展示模式、排序字段、排序方向、状态筛选、走势展开状态、走势时间宽度和纵轴模式。
- Webview 初始化时校验并恢复已保存的 UI 状态，避免旧值或异常值破坏渲染。
- `registerWebviewViewProvider` 启用 `retainContextWhenHidden`，减少切换其他 VSCode 面板时 Webview 上下文被销毁的概率。

### PRD 更新
- 更新直播监控模块文档中的控制面板状态保留规则。
- 更新架构文档中的 Webview 状态保存和上下文保留说明。
- 更新产品概览和 README 的当前能力描述。

### 验证
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- `getState` / `setState` 保存的是当前 Webview 会话状态，不作为跨设备或长期配置同步；跨 VSCode 窗口、重新安装扩展或清理工作台状态后可能恢复默认值。

## 2026-08-12 - 优化走势图纵轴范围标注

### 编写人
- dumpling

### 请求
- 用户确认走势图左下角的 `987-1,035` 类数字表示纵轴范围，但当前合并显示不清晰。
- 用户希望按照截图标注的位置，用更直观的两个框展示纵轴范围。

### 影响模块
- Webview 在线人数迷你走势图。
- 直播监控模块 PRD。

### 代码变更
- 将走势图原有 `min-max` 合并文本拆成左侧上下两个纵轴标签。
- 上方标签显示当前纵轴最大值，下方标签显示当前纵轴最小值。
- 底部图例只保留时间范围，并调整图表左侧内边距避免标签与折线重叠。

### PRD 更新
- 更新直播监控模块文档中的走势图轴标展示规则。

### 验证
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 极大在线人数的完整数字可能在窄侧栏中被省略号截断；当前先保证轴标位置直观和不遮挡折线。

## 2026-08-12 - 增加走势图同步悬浮参考线

### 编写人
- dumpling

### 请求
- 用户希望在展开走势图时，鼠标悬浮到某个主播走势图范围内，所有图相同竖向位置展示一根竖线。
- 用户希望竖线与每个走势图相交，并显示每个图在该时间的具体在线人数。

### 影响模块
- Webview 在线人数迷你走势图交互。
- 直播监控模块 PRD。

### 代码变更
- 新增全局 `hoverTimestamp`，鼠标悬浮任意走势图时按当前窗口横向比例计算统一时间点。
- 所有展开走势图基于同一个悬浮时间点绘制 SVG 竖向参考线。
- 每个图查找距离该时间最近的有效在线人数采样点，绘制交点标记并显示具体在线人数。
- 鼠标离开走势图区域时清除悬浮参考线和值标签。

### PRD 更新
- 更新直播监控模块文档中的同步悬浮参考线和值标签规则。

### 验证
- 已运行：`npm.cmd test`，21 个测试通过。
- 待运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`。
- 待运行：`npx.cmd vsce package`。

### 风险/后续事项
- 悬浮值使用最近有效采样点，而不是在两点间做线性插值；如果某个主播某段时间采样缺失，显示值可能对应参考线附近的最近采样。

## 2026-08-12 - 调整走势图默认窗口为 10 分钟

### 编写人
- dumpling

### 请求
- 用户要求走势时长选择默认为最近 10 分钟，而不是默认 6 小时。

### 影响模块
- Webview 走势控制区。
- 直播监控模块 PRD、产品概览和 README。

### 代码变更
- 新增 `TREND_WINDOW_DEFAULT_MINUTES`，默认值使用最小窗口 10 分钟。
- Webview 无已保存状态或保存值无效时，走势窗口回退到最近 10 分钟。
- HTML 初始滑杆文案和值从“最近 6 小时 / 360”改为“最近 10 分钟 / 10”。
- 已保存的用户手动选择仍通过 Webview 状态恢复，不被默认值覆盖。

### PRD 更新
- 更新直播监控模块文档中的走势图默认窗口规则。
- 更新产品概览和 README 中的默认展示窗口说明。

### 验证
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 如果当前 Webview 已保存过 6 小时或其他窗口宽度，仍会优先恢复用户已保存选择；清理 Webview 状态或首次打开时才使用新的 10 分钟默认值。

## 2026-08-12 - 回退走势图同步悬浮参考线

### 编写人
- dumpling

### 请求
- 用户重新评估后认为同步悬浮参考线和值标签展示仍不直观，要求先回退该功能，稍后提出另一种方案。

### 影响模块
- Webview 在线人数迷你走势图交互。
- 直播监控模块 PRD。

### 代码变更
- 移除走势图 `pointermove` / `pointerleave` 悬浮交互。
- 移除全局 `hoverTimestamp`、同步竖线、交点标记和悬浮数值标签绘制逻辑。
- 移除悬浮竖线、交点和悬浮文字相关 CSS。
- 保留走势图本体、上下纵轴标签、时间范围、默认 10 分钟窗口、控制状态保留等其它能力。

### PRD 更新
- 从直播监控模块当前行为中移除同步悬浮参考线和值标签规则。

### 验证
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 后续新方案提出前，走势图不提供鼠标悬浮读数能力。

## 2026-08-12 - 移除统一纵轴切换

### 编写人
- dumpling

### 请求
- 用户要求去掉控制面板中的“独立 / 统一”纵轴范围功能，走势图按各自独立形式展示。

### 影响模块
- Webview 走势控制区。
- Webview 在线人数迷你走势图缩放逻辑。
- 直播监控模块 PRD。

### 代码变更
- 移除控制面板中的 `独立` / `统一` 纵轴切换按钮。
- 移除 `trendAxisMode`、轴模式按钮事件、轴模式持久化字段和共享纵轴 `getSharedTrendScale()` 逻辑。
- 走势图生成时始终使用当前主播自身窗口内有效在线人数点计算 min/max。

### PRD 更新
- 更新直播监控模块文档，将纵轴行为改为始终按每个主播独立缩放。

### 验证
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 不同主播走势图的纵轴范围不再统一，跨主播比较绝对波动幅度时需要结合左侧上下轴标读取范围。

## 2026-08-12 - 新增直播中主播总览走势图

### 编写人
- dumpling

### 请求
- 用户希望在控制面板新增按钮，展开一个新的大走势图。
- 大图只展示目前正在开播的主播，多条在线人数走势用不同颜色叠加，并在下方展示颜色图例。
- 大图拥有独立于单主播迷你走势图的时间范围控制。
- 鼠标悬浮大图时，通过浮层展示当前时间位置附近各主播的名字和在线人数。

### 影响模块
- Webview 控制面板。
- Webview 在线人数走势图渲染与交互。
- 直播监控模块 PRD、产品概览、架构文档和 README。

### 代码变更
- 在控制面板新增“总览”按钮，并在汇总区与主播列表之间新增总览走势图挂载区域。
- Webview 状态新增总览展开状态和总览时间窗口宽度，并通过 `getState` / `setState` 保持切换侧栏后的 UI 状态。
- 新增直播中主播总览 SVG 图：只纳入 `status === live` 的房间，用不同颜色绘制同一坐标系下的在线人数曲线。
- 总览图新增独立时间范围滑杆，范围为 10 分钟到 6 小时，默认 10 分钟，不影响单主播迷你走势图。
- 总览图下方展示颜色图例；鼠标悬浮时显示竖向参考线、交点和浮层，浮层按当前横向时间位置列出各主播最近有效采样点的在线人数。
- 数据不足或无直播中主播时显示可恢复的占位状态，不影响主播列表展示。

### PRD 更新
- 更新直播监控模块文档中的总览走势交互、时间窗口、图例、悬浮读数和纵轴规则。
- 更新产品概览中的核心场景与非目标边界。
- 更新架构文档中的 Webview SVG 数据流。
- 更新 README 当前能力说明。

### 验证
- 已运行：`node --check media/webview.js`，语法检查通过。
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 悬浮读数使用离当前鼠标时间位置最近的有效采样点，不做两点间插值；如果采样间隔较大，显示值可能来自参考线附近的最近刷新点。
- 总览大图使用统一纵轴，适合比较绝对在线人数；如果主播在线人数差异极大，低在线人数曲线的局部波动会更不明显。

## 2026-08-12 - 优化走势图横向自适应

### 编写人
- dumpling

### 请求
- 用户反馈 VSCode 插件窗口横向拉宽后，走势图仍像固定宽度居中显示，左右空白过多，且与轴坐标数据观感分离。
- 希望窗口横向缩放能够影响走势图横轴宽度，使工具看起来更智能，同时保留走势图最小宽度。

### 影响模块
- Webview 在线人数迷你走势图。
- Webview 直播中主播总览走势图。
- 直播监控模块 PRD、产品概览、架构文档和 README。

### 代码变更
- 为单主播迷你走势图 SVG 增加 `preserveAspectRatio="none"`，让横轴随容器宽度非等比拉伸，不再按固定 viewBox 比例居中留白。
- 为单主播走势图区域增加最小宽度和局部横向滚动；当 VSCode 侧栏过窄时优先保留图表可读性。
- 为总览走势图外层面板增加最小宽度和局部横向滚动；宽窗口下总览图继续填满可用宽度，窄窗口下不挤碎曲线和图例。
- 保持总览图已有的横向非等比拉伸、图例、悬浮读数和独立时间窗口逻辑不变。

### PRD 更新
- 更新直播监控模块文档中的走势图横轴自适应和最小宽度规则。
- 更新产品概览中的走势图横向缩放体验说明。
- 更新架构文档中的 Webview SVG 自适应策略。
- 更新 README 当前能力说明。

### 验证
- 已运行：`node --check media/webview.js`，语法检查通过。
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 横向非等比拉伸只改变走势图横轴视觉密度，不改变历史采样时间窗口或数据计算。
- 当侧栏宽度低于最小可读宽度时，会出现图表区域内横向滚动；这是为了避免曲线和轴标被过度压缩。

## 2026-08-12 - 重排控制面板视觉分组

### 编写人
- dumpling

### 请求
- 用户反馈控制面板里临时添加的内容比较混乱，希望按重要程度、功能范围、美观程度和直观程度重新排版。
- 本次只做视觉层面的优化，不影响实际功能。

### 影响模块
- Webview 控制面板 HTML 结构。
- Webview 控制面板 CSS 样式。
- 直播监控模块 PRD、产品概览和架构文档。

### 代码变更
- 将展开控制面板重排为“视图”“排序”“刷新”“走势”四个视觉分组。
- “视图”组保留展示模式切换和主播状态筛选，突出一次性影响列表展示形式的控制。
- “排序”组只保留排序字段和排序方向。
- “刷新”组收束自动刷新、刷新间隔和开播提醒。
- “走势”组收束单主播小图、直播中主播总览和小图时间范围控制。
- 保留原有按钮和表单元素 id，不改变 Webview JS 事件绑定、消息类型、配置项和状态持久化逻辑。
- 增加窄侧栏下的单列响应式布局，避免控制面板内容拥挤。

### PRD 更新
- 更新直播监控模块文档中的控制面板分组规则。
- 更新产品概览中的控制面板使用说明。
- 更新架构文档中控制面板分组但复用原有 DOM id 与消息类型的说明。

### 验证
- 已运行：`node --check media/webview.js`，语法检查通过。
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 本次没有新增或删除功能，只调整控制面板视觉层级和响应式布局；如后续新增控制项，应优先归入现有四组，避免再次堆叠。

## 2026-08-12 - 将走势图坐标范围并入图表轴

### 编写人
- dumpling

### 请求
- 用户反馈走势图的横轴时间范围和纵轴数值范围像图外标签，与折线区域分离。
- 希望坐标范围作为走势图坐标轴的一部分展示。

### 影响模块
- Webview 在线人数迷你走势图。
- Webview 直播中主播总览走势图。
- 直播监控模块 PRD、产品概览和架构文档。

### 代码变更
- 将单主播迷你走势图的纵轴最大/最小值、横轴起止时间从 HTML 绝对定位标签改为 SVG 内部文本。
- 单主播迷你走势图 SVG 内部新增横轴和纵轴线，折线、坐标范围和坐标轴共享同一个 viewBox。
- 将总览走势图的纵轴最大/最小值、横轴起止时间也改为 SVG 内部文本，移除外置坐标标签。
- 调整总览图悬浮时间映射，按 SVG 绘图区范围计算横向比例，避免坐标轴内边距影响读数。
- 清理旧的外置坐标标签 CSS，新增 SVG 轴线和轴文字样式。

### PRD 更新
- 更新直播监控模块文档中的坐标轴展示规则。
- 更新产品概览中的走势图坐标范围展示说明。
- 更新架构文档中的 Webview SVG 坐标绘制策略。

### 验证
- 已运行：`node --check media/webview.js`，语法检查通过。
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 当前 SVG 使用横向非等比拉伸以适配侧栏宽度，坐标文字也会随 SVG 横向缩放；若后续观察到文字变形明显，可进一步改为 resize 后动态计算 SVG 尺寸。

## 2026-08-12 - 修复走势图横向缩放形变

### 编写人
- dumpling

### 请求
- 用户反馈插件窗口缩放会导致走势图线条粗细和坐标文字/数字变形。
- 用户希望窗口宽度变化只改变走势图横轴宽度和点位密度，时间范围不变，线条和文字保持正常比例。

### 影响模块
- Webview 在线人数迷你走势图。
- Webview 直播中主播总览走势图。
- 直播监控模块 PRD、产品概览和架构文档。

### 代码变更
- 移除走势图 SVG 的横向非等比拉伸策略，不再使用固定 viewBox 配合 `preserveAspectRatio="none"` 适配宽度。
- 新增响应式 SVG 重绘逻辑，通过 `ResizeObserver` 按当前图表容器像素宽度重新计算 viewBox、坐标轴和折线路径。
- 单主播迷你走势图和直播中主播总览走势图均保留最小 360px 可读宽度；窄侧栏下继续由图表区域横向滚动。
- 总览图悬浮读数改为基于实际 SVG 区域计算横向时间位置，避免容器边框或宽度变化影响读数定位。
- 为折线、轴线和参考线补充 `vector-effect: non-scaling-stroke` 作为线宽稳定兜底。

### PRD 更新
- 更新直播监控模块文档中的走势图横轴自适应规则，明确按容器宽度重绘且线条/文字不随横向缩放形变。
- 更新产品概览中的走势图横向缩放体验说明。
- 更新架构文档中的 Webview SVG 自适应策略，将旧的非等比拉伸改为 `ResizeObserver` 动态重建坐标。

### 验证
- 已运行：`node --check media/webview.js`，语法检查通过。
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 侧栏宽度低于最小 360px 时仍会出现图表区域内横向滚动，这是为了保证坐标文字和折线不会被压缩变形。

## 2026-08-12 - 修复走势时间滑杆拖拽中断

### 编写人
- dumpling

### 请求
- 用户反馈总览走势图的时间宽度滑杆无法自由拖动。

### 影响模块
- Webview 走势控制区。
- Webview 单主播迷你走势图时间窗口控制。
- Webview 直播中主播总览走势图时间窗口控制。
- 直播监控模块 PRD。

### 代码变更
- 将单主播走势时间滑杆的 `input` 事件改为只更新滑杆值和“最近 N 分钟/小时”标签，不再立即重渲染整个 Webview。
- 将总览走势图独立时间滑杆的 `input` 事件改为只更新标签，不再在拖动过程中销毁并重建当前 range 控件。
- 新增提交函数，在滑杆 `change` 事件触发时写入对应时间窗口状态并重绘走势图。
- 复用 `formatTrendWindowMinutesLabel()` 生成滑杆拖动过程中的即时标签文案。

### PRD 更新
- 更新直播监控模块文档，记录时间宽度滑杆拖动过程中只更新标签，松开或提交后再重绘走势图，避免拖拽中断。

### 验证
- 已运行：`node --check media/webview.js`，语法检查通过。
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 拖动过程中标签会先反映目标窗口宽度，图表会在松开或提交后更新；这是为了保证原生 range 控件拖拽不中断。

## 2026-08-12 - 总览走势图图例支持隐藏曲线

### 编写人
- dumpling

### 请求
- 用户要求综合走势图下方图例支持点击对应主播，隐藏或重新加入上方走势图。
- 隐藏状态需要在图例文字上画删除线表示。

### 影响模块
- Webview 总览走势图渲染与交互。
- Webview UI 状态持久化。

### 实现变化
- 为 Webview 增加总览图例隐藏房间集合，并通过 `getState` / `setState` 保持切换面板后的 UI 状态。
- 总览图例从纯展示项改为可点击按钮；点击后隐藏或恢复对应主播曲线，隐藏项降低透明度并给主播名加删除线。
- 总览大图绘制、纵轴 min/max 计算和悬浮浮层只使用当前可见主播；图例始终保留全部直播中主播，避免全部隐藏后无法恢复。
- 当全部直播中主播都被隐藏时，总览区域显示“已隐藏全部主播”，并保留图例用于重新加入曲线。

### PRD 更新
- 更新直播监控模块文档，记录总览图例隐藏/恢复曲线、删除线隐藏态、Webview 状态保存和可见曲线纵轴计算规则。
- 更新产品概览中的总览走势图能力说明。
- 更新架构文档中的 Webview 总览图例状态和绘制数据流。

### 验证
- 已运行：`node --check media/webview.js`，语法检查通过。
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\\skills\\prd-keeper\\scripts\\check_prd_keeper.py --log-file docs\\prd\\prd_log\\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\\self-work\\bwatch\\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 隐藏状态只影响总览大图曲线显示，不影响监控列表、单主播迷你走势图、刷新任务或历史缓存。

## 2026-08-12 - 总览走势图增加候选范围切换

### 编写人
- dumpling

### 请求
- 用户要求综合图默认展示所有关注的直播间数据，而不是只展示开播直播间。
- 用户要求复用图例隐藏/恢复曲线能力，并在综合图上方新增三个配置按钮：展示所有直播间、展示开播的直播间、展示选择时间范围内有数据的直播间。

### 影响模块
- Webview 总览走势图候选数据范围。
- Webview 总览走势图控制区布局。
- Webview UI 状态持久化。

### 实现变化
- 总览图标题从“直播中主播总览”调整为“主播总览”，默认候选范围改为全部关注直播间。
- 在总览图头部新增 `全部`、`开播`、`有数据` 三个范围按钮；`有数据` 按当前总览时间窗口内存在有效数字采样点判断，`null` 断线采样不计入有效数据。
- 总览候选范围通过 Webview `getState` / `setState` 保存，切换 VSCode 面板后不重置。
- 图例隐藏/恢复曲线继续只影响当前候选范围内的曲线显示；隐藏集合按监控列表清理，不因切换总览范围丢失隐藏状态。
- 曲线颜色按监控列表顺序稳定分配，减少在不同候选范围之间切换时同一主播颜色变化。

### PRD 更新
- 更新直播监控模块文档，记录总览默认全部关注、三种候选范围、`有数据` 判定和 Webview 状态保存规则。
- 更新产品概览中的总览走势图能力描述。
- 更新架构文档中的 Webview 总览候选范围和图例隐藏数据流。

### 验证
- 已运行：`node --check media/webview.js`，语法检查通过。
- 已运行：`npm.cmd test`，21 个测试通过。
- 已运行：`python .agent\\skills\\prd-keeper\\scripts\\check_prd_keeper.py --log-file docs\\prd\\prd_log\\20260812-dumpling.md`，校验通过。
- 已运行：`npx.cmd vsce package`，生成 `D:\\self-work\\bwatch\\bwatch-0.1.0.vsix`。

### 风险/后续事项
- 全部关注模式下，未开播直播间会以刷新采样中的 `0` 参与走势；如果某个房间当前窗口内有效点不足两点，总览仍会在图例保留该主播，但不足以单独形成折线。
