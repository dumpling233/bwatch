# 2026-08-28 场次峰值高度控件位置调整

### 编写人

dumpling

### 请求

将场次峰值图表高度滑杆放到历史走势控制面板最上方，使用户调整高度后可以从主播信息区域开始截图，不把高度控件截入图片。

### 影响模块

- `media/webview.js`：历史走势控制区和场次峰值区块的控件排列顺序。
- `src/test/webviewRuntime.test.ts`：高度控件位置回归测试。
- `docs/prd/modules/live-monitor.md`：场次峰值高度控件布局规则。
- `docs/prd/README.md`：最近 PRD 日志索引。

### 代码变更

- 历史走势控制面板展开后，首项渲染场次峰值图表高度滑杆。
- “主播 / 直播场次”级联选择区移动到高度滑杆之后。
- 场次峰值区块内部只保留标题、最近 10 场/30 场/全部范围按钮和图表。
- 高度范围、默认值、调整方式和 Webview 状态持久化行为保持不变。
- 增加结构测试，确保高度控件位于主播选择区之前，且场次峰值区块不重复渲染该控件。

### PRD 更新

- 更新 `docs/prd/modules/live-monitor.md`。
- 更新 `docs/prd/README.md` 日志索引。

### 验证

- `npm.cmd test`：通过，TypeScript 编译及 148 项测试全部成功。
- `git diff --check`：通过。
- PRD keeper：通过。
- `npx.cmd vsce package --out bwatch-0.1.0.vsix`：通过，生成 73 个文件、约 181.62 KB 的 VSIX。

### 风险/后续事项

- 高度滑杆常驻历史走势控制面板顶部，即使尚未选择主播也可预先调整高度。
- 本次仅调整展示顺序，不改变场次峰值数据、图表绘制或截图功能本身。
