# 2026-08-13 PRD 日志 - dumpling

### 编写人

dumpling

### 请求

用户要求先改造本地长期历史数据保存能力：在线人数数据需要持久保存在本地，后续用于历史查询；删除监控房间后不删除该房间历史，重新添加同一房间后可以恢复使用旧数据。

### 影响模块

| 模块 | 影响 |
|---|---|
| 直播监控扩展 | 在线人数历史从最近 24 小时 `globalState` 缓存升级为本地长期历史文件。 |
| 架构说明 | 数据层边界改为 VSCode 扩展本地存储目录持久化，并保留近期快照裁剪。 |
| PRD 总入口/产品概览 | 更新在线人数历史的长期保存、删除不清历史和当前非目标边界。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `src/onlineHistoryStore.ts` | 按房间号拆分保存 JSON 历史文件；初始化加载本地历史；迁移旧 `bwatch.onlineHistory.v1`；`pruneRooms`/`deleteRoom` 不再删除长期历史；快照查询仍裁剪最近 24 小时。 |
| `src/extension.ts` | 创建 `OnlineHistoryStore` 时传入 `context.globalStorageUri.fsPath`。 |
| `src/liveMonitor.ts` | 初始快照和设置变更时按当前监控房间过滤历史，避免已移除房间出现在 UI 快照中。 |
| `src/test/onlineHistoryStore.test.ts` | 覆盖本地文件持久化、长期历史保留、删除不清理和旧 `globalState` 迁移。 |
| `src/test/liveMonitor.test.ts` | 更新移除房间语义，并新增重新添加后恢复历史的测试。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/README.md` | 当前目标改为本地长期保存在线人数采样，并新增当天日志入口。 |
| `docs/prd/product-overview.md` | 增加长期保存、删除不清历史、重新添加恢复历史，以及当前不提供历史查询/导出/云同步的边界。 |
| `docs/prd/modules/live-monitor.md` | 更新在线历史的数据生命周期、快照边界、旧数据迁移和删除房间行为。 |
| `docs/prd/architecture.md` | 更新数据层、目录映射和关键数据流。 |

### 验证

| 命令 | 结果 |
|---|---|
| `npm.cmd test` | 通过，23 个测试全绿。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 历史文件增长 | 当前先满足长期保存，不做压缩、归档、清理策略或容量上限；后续历史查询功能设计时需要补充数据管理规则。 |
| UI 查询能力 | 当前 Webview 快照仍只携带最近 24 小时绘图数据；完整历史查询页面尚未实现。 |

## 走势图时间条粒度改为 1 分钟

### 编写人

dumpling

### 请求

用户要求两个走势时间条设置的粒度从 10 分钟改为 1 分钟。

### 影响模块

| 模块 | 影响 |
|---|---|
| Webview 走势控制 | 单主播迷你走势图和总览大图的横轴时间宽度滑杆最小值、步长和默认值调整为 1 分钟。 |
| 直播监控 PRD | 当前走势图窗口范围从“最近 10 分钟到 6 小时”改为“最近 1 分钟到 6 小时”。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `media/webview.js` | `TREND_WINDOW_MIN_MINUTES` 改为 `1`；单主播与总览滑杆的步长统一为 `1`。 |
| `src/webviewProvider.ts` | 静态 HTML 初始标签和 range 属性改为最近 1 分钟、`min=1`、`step=1`、`value=1`。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/product-overview.md` | 更新走势图默认窗口和可选范围描述。 |
| `docs/prd/modules/live-monitor.md` | 更新单主播迷你图、总览图和快照绘图窗口范围描述。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，23 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 已保存 Webview 状态 | 旧版本保存过的 10 分钟或其它合法窗口值仍会按用户状态恢复；首次打开或状态无效时默认使用 1 分钟。 |

## 本地历史在线人数按日期查询

### 编写人

dumpling

### 请求

用户要求新增按日期查询本地存储历史在线人数走势图：提供日历组件，只允许选择有本地数据的日期；选择日期后可调整当天 `00:00` 到 `23:59` 内的开始和结束时间；走势图展示与筛选参考总览图，但历史场景不提供是否开播筛选，只提供是否有数据筛选，且过滤时间段内数据全为 `0` 的主播不应纳入“有数据”范围。

### 影响模块

| 模块 | 影响 |
|---|---|
| Webview 走势控制 | 新增“历史”按钮和本地历史查询面板。 |
| 在线历史存储 | 新增有数据日期列表和按日期时间段查询能力。 |
| Webview 消息桥 | 新增 `loadHistoryDates`、`loadHistoryDate` 请求和对应返回消息。 |
| 直播监控 PRD/架构 | 增加历史查询 UI、数据流和筛选规则描述。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `src/types.ts` | 新增 `HistoryDateSummary`、`HistoryRoomSeries`、`HistoryQueryResult`。 |
| `src/onlineHistoryStore.ts` | 新增 `getAvailableDates()` 和 `queryDateHistory()`；按本地自然日统计和查询长期历史采样。 |
| `src/webviewProvider.ts` | 新增历史查询消息类型和扩展宿主回传逻辑。 |
| `src/extension.ts` | 将 `OnlineHistoryStore` 历史查询能力接入 Webview actions，并把当前已知主播名传给历史结果。 |
| `src/webviewProvider.ts` | HTML 中新增“历史”按钮和历史走势图容器。 |
| `media/webview.js` | 新增历史面板状态、日期加载、日期快捷按钮、起止时间输入、全部/有数据筛选、历史叠加走势图、历史图例隐藏/恢复。 |
| `media/webview.css` | 新增历史面板、日期列表、时间输入和三列走势按钮样式。 |
| `src/test/onlineHistoryStore.test.ts` | 新增有数据日期列表和按日期时间段查询测试。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/product-overview.md` | 增加历史查询核心场景与边界。 |
| `docs/prd/modules/live-monitor.md` | 增加历史面板、日历、时间段、筛选和数据查询规则。 |
| `docs/prd/architecture.md` | 增加历史查询消息流和 `OnlineHistoryStore` 查询职责。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，25 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 日历原生能力 | 当前使用原生 `input[type=date]` 加有数据日期快捷按钮；原生日历弹层无法禁用无数据日期，但输入变更会被校验回退，快捷按钮只展示有数据日期。 |
| 历史主播名 | 历史文件目前只保存房间号和在线人数，历史结果优先使用当前监控快照中已知主播名；缺少映射时显示房间号。 |

## 历史查询时间范围改为双端范围条

### 编写人

dumpling

### 请求

用户反馈历史查询面板中的开始/结束时间输入不直观，希望改成类似双端可拖动范围条或底部时间范围选择条，范围固定为当日 `00:00` 到 `23:59`。

### 影响模块

| 模块 | 影响 |
|---|---|
| Webview 历史查询面板 | 起止时间从两个 `time` 输入框改为一个当日双端范围条。 |
| 直播监控 PRD | 更新历史时间范围选择交互描述。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `media/webview.js` | 新增历史范围条 DOM、双手柄拖动预览、提交时范围归一化和 Webview 状态恢复归一化；拖动时只更新标签和高亮区，松开后请求历史数据。 |
| `media/webview.css` | 新增历史范围条轨道、高亮段、双手柄、端点标签样式，并给历史面板时间区域更大的横向空间。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/product-overview.md` | 将历史查询起止时间描述更新为当日双端范围条。 |
| `docs/prd/modules/live-monitor.md` | 明确历史范围条固定 `00:00` 到 `23:59`，拖动时即时更新标签，提交后查询包含结束整分钟的本地历史。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，25 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 原生 range 双手柄 | 当前用两个重叠的原生 range 实现双端范围条；不同 VSCode Webview 内核的手柄细节可能略有差异，但范围值和查询逻辑保持一致。 |

## 主播搜索 HTTP 412 兼容修复

### 编写人

dumpling

### 请求

用户反馈添加弹窗中按主播名搜索直播间时经常报错，截图中显示“B站主播搜索接口返回 HTTP 412”，希望修复搜索功能稳定性。

### 影响模块

| 模块 | 影响 |
|---|---|
| B站接口适配 | 主播搜索请求增加浏览器兼容请求头、搜索页 `referer`、访客 cookie 和更完整的搜索参数。 |
| 添加弹窗 | 数字房间号不再调用主播搜索接口；中文输入法组合输入期间不搜索；非数字主播名少于 2 个字符时不请求接口。 |
| PRD/架构 | 更新主播搜索失败处理、请求头和 HTTP 412 可恢复错误规则。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `src/bilibiliClient.ts` | 为 B站请求统一生成浏览器兼容请求头；主播搜索接口补充 `page_size`、`order`、`platform`、`highlight`、`single_column`、`from_source` 参数；HTTP 412 或错误码 `-412` 时返回友好错误。 |
| `src/extension.ts` | 纯数字输入只返回本地房间号候选并直接跳过主播搜索接口；主播搜索异常以可读错误继续回传 Webview。 |
| `media/webview.js` | 添加弹窗支持中文输入法 composition 期间不搜索、短主播名不搜索、防抖延长到 500ms，并在清空/关闭弹窗时忽略迟到搜索结果。 |
| `src/test/bilibiliClient.test.ts` | 新增主播搜索浏览器请求头和 412 友好错误测试。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/modules/live-monitor.md` | 更新添加弹窗搜索触发条件、数字房间号绕开搜索接口、中文输入法和搜索失败处理规则。 |
| `docs/prd/architecture.md` | 更新主播搜索外部服务请求头和 412 可恢复错误边界。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，27 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| B站搜索风控 | HTTP 412 本质上是外部非正式接口的拦截/风控表现，浏览器化请求和降频只能降低触发概率，不能保证永久可用。 |
| 房间号兜底 | 当主播搜索继续被拦截时，用户仍可直接输入直播间房间号添加监控。 |

## 历史走势图主播名兜底修复

### 编写人

dumpling

### 请求

用户反馈历史走势图中部分组件字段显示异常，图例和悬浮窗显示房间号数字而不是主播名，希望修复展示字段。

### 影响模块

| 模块 | 影响 |
|---|---|
| 在线历史存储 | 长期历史文件增加最近已知主播名元数据，历史查询在缺少当前快照映射时仍可返回主播名。 |
| 历史走势图 | 图例和悬浮窗继续使用 `HistoryRoomSeries.anchorName`，但后端返回值不再只依赖当前监控列表。 |
| PRD/架构 | 更新本地历史文件格式、主播名兜底优先级和旧格式兼容规则。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `src/onlineHistoryStore.ts` | 本地历史文件新写入 `{ version, anchorName, points }`；兼容读取旧数组格式；刷新记录时保存有效主播名；历史查询按当前快照名、本地历史名、房间号顺序兜底；接口失败返回的 `-`、空值或 `房间 {roomId}` 不覆盖已有主播名。 |
| `src/test/onlineHistoryStore.test.ts` | 新增历史查询使用本地主播名、重启恢复主播名、兜底值不覆盖有效主播名的测试。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/product-overview.md` | 补充历史查询图例和悬浮窗优先显示主播名，缺少主播名时才显示房间号。 |
| `docs/prd/modules/live-monitor.md` | 补充历史文件保存最近已知主播名、旧格式兼容和 `HistoryRoomSeries.anchorName` 兜底规则。 |
| `docs/prd/architecture.md` | 补充 `OnlineHistoryStore` 主播名元数据职责、数据流和历史查询返回名称优先级。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，30 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 旧历史名称补齐 | 旧历史文件如果从未重新刷新过对应房间，文件中仍没有主播名；下一次该房间成功刷新并拿到有效主播名后会写入并用于后续历史查询。 |

## 添加弹窗搜索结果已监控状态

### 编写人

dumpling

### 请求

用户希望添加弹窗的搜索结果列表与当前已监控直播间列表进行对比，如果候选直播间已经在监控列表中，则在搜索结果中展示已监控状态。

### 影响模块

| 模块 | 影响 |
|---|---|
| 添加弹窗 | 搜索结果增加已监控状态展示，已监控候选不可重复点击添加。 |
| Webview 消息数据 | `RoomSearchResult` 增加 `monitored` 字段，由扩展宿主基于 `bwatch.rooms` 计算。 |
| PRD/架构 | 更新添加弹窗候选状态、数据字段和搜索结果数据流。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `src/types.ts` | `RoomSearchResult` 增加 `monitored: boolean` 字段。 |
| `src/extension.ts` | `searchRooms()` 读取当前 `bwatch.rooms`，为房间号候选和主播搜索候选标记是否已监控；命令面板 quick pick 也在描述中展示已监控。 |
| `media/webview.js` | 搜索结果统计已监控数量；已监控结果行显示 `已监控` 徽标、禁用点击并不发送重复添加消息。 |
| `media/webview.css` | 增加已监控搜索结果行和徽标样式。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/product-overview.md` | 补充添加弹窗候选结果会标识已监控并避免重复添加。 |
| `docs/prd/modules/live-monitor.md` | 补充添加弹窗搜索结果与 `bwatch.rooms` 对比、`RoomSearchResult.monitored` 字段和禁用重复添加行为。 |
| `docs/prd/architecture.md` | 补充扩展宿主负责候选已监控标记、Webview 展示并禁用重复添加的数据流。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，30 个测试全绿。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 配置实时性 | 搜索结果的已监控状态按搜索请求发生时的 `bwatch.rooms` 计算；如果弹窗打开期间外部设置被修改，需要重新搜索才能看到最新标记。 |

## 自定义分组与图表分组筛选

### 编写人

dumpling

### 请求

用户希望新增自定义分组能力：可以创建/删除分组，并在主列表两种展示模式下按固定“全部”分组和自定义分组折叠/展开展示；分组结果也要作为总览聚合走势图和历史聚合走势图的筛选条件。

### 影响模块

| 模块 | 影响 |
|---|---|
| 配置读取 | 新增 `bwatch.groups`，并对分组 id、名称和成员房间号做标准化。 |
| 扩展宿主 | 新增创建分组、删除分组和设置房间分组归属的 Webview 消息处理。 |
| Webview 主列表 | 详细模式和简略模式按固定“全部”分组加自定义分组树状展示，并支持折叠/展开。 |
| 总览/历史走势图 | 候选范围按钮追加每个自定义分组，支持按分组筛选曲线。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `src/types.ts` | 新增 `RoomGroup`，`MonitorSettings` 增加 `groups`。 |
| `src/config.ts` | 新增 `normalizeRoomGroups()`，读取配置时过滤无效分组和非当前监控房间成员。 |
| `src/extension.ts` | 新增 `createGroup`、`deleteGroup`、`setRoomGroups`，删除房间时同步移除分组成员关系。 |
| `src/webviewProvider.ts` | 新增分组控制区 HTML 和分组相关消息类型。 |
| `media/webview.js` | 新增分组控制、主列表分组折叠展示、房间分组归属编辑、总览/历史图分组 scope。 |
| `media/webview.css` | 新增分组控制、树状分组列表、分组归属编辑和图表筛选按钮自适应样式。 |
| `package.json` | 新增 `bwatch.groups` 配置声明。 |
| `src/test/config.test.ts` | 增加分组配置标准化和 `readMonitorSettings` 读取分组测试。 |
| `src/test/liveMonitor.test.ts` | 补齐 `MonitorSettings.groups` 字段。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/product-overview.md` | 增加自定义分组核心场景、图表分组筛选和删除房间不删历史规则，并移除旧的“不提供分组”边界。 |
| `docs/prd/modules/live-monitor.md` | 补充分组交互、配置契约、数据状态和总览/历史图分组筛选规则。 |
| `docs/prd/architecture.md` | 补充分组配置、数据流、Webview 状态和消息桥接说明。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，32 个测试全绿。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 分组排序 | 当前版本只提供创建、删除、归属管理和折叠展示，不提供分组拖拽排序。 |
| 多分组展示 | 同一主播可属于多个自定义分组，因此会同时出现在“全部”和多个自定义分组中；图表选择具体分组时只按该分组过滤。 |

## 聚合走势图有数据筛选与图例隐藏状态修正

### 编写人

dumpling

### 请求

用户希望两个聚合走势图的“有数据”筛选结果只包含所选时间区间内存在不为 0 在线人数数据的直播间，不要把只有 0 的直播间放入结果；同时点击对应筛选标签后，需要清空下方图例的隐藏/开启状态，恢复默认全显示。

### 影响模块

| 模块 | 影响 |
|---|---|
| 总览聚合走势图 | “有数据”从有效数字采样收紧为当前总览时间窗口内存在大于 0 在线人数采样。 |
| 历史聚合走势图 | 保持大于 0 的“有数据”口径，并在筛选标签点击时清空图例隐藏状态。 |
| PRD/架构 | 明确总览和历史候选范围、图例隐藏状态重置规则。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `media/webview.js` | `roomHasValidOverviewPoint()` 改为要求 `online > 0`；总览和历史范围按钮点击时清空对应 hidden room 集合，即使重复点击当前选中标签也会重绘恢复全显示。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/product-overview.md` | 补充总览“有数据”只纳入大于 0 的在线人数采样，并记录范围标签点击会恢复图例全显示。 |
| `docs/prd/modules/live-monitor.md` | 补充总览/历史“有数据”口径和候选范围按钮重置图例隐藏状态规则。 |
| `docs/prd/architecture.md` | 更新 Webview 总览与历史候选范围、图例隐藏集合清理数据流描述。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，32 个测试全绿。 |
| `python .agent\\skills\\prd-keeper\\scripts\\check_prd_keeper.py --log-file docs\\prd\\prd_log\\20260813-dumpling.md` | 首次因本节验证占位失败；补全验证结果后通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\\self-work\\bwatch\\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| Webview 单元覆盖 | 当前修正集中在 Webview 交互脚本，现有自动化测试主要覆盖扩展宿主和数据层；通过语法检查、测试套件和打包验证兜底。 |

## 管理面板图标化与 VSCode 原生输入弹窗

### 编写人

dumpling

### 请求

用户希望再次精简管理面板：搜索添加直播间和新建分组参考工具栏 icon 形式展示，并希望输入弹窗调用 VSCode 自带弹窗能力。

### 影响模块

| 模块 | 影响 |
|---|---|
| Webview 顶部管理区 | 顶部常驻区改为添加、刷新、新建分组、控制面板四个图标按钮。 |
| 添加直播间 | 从 Webview 内自制搜索弹窗改为 VSCode 原生 `showInputBox` + `showQuickPick`。 |
| 自定义分组 | 新建分组从控制面板内输入框改为顶部图标触发 VSCode 原生 `showInputBox`；控制面板分组区只保留已有分组管理。 |
| PRD/README | 更新当前添加、搜索、新建分组和管理面板描述。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `src/webviewProvider.ts` | Webview 消息改为 `openAddRoomPicker` 和 `openCreateGroupInput`；移除 Webview 搜索消息处理与自制搜索弹窗 HTML；顶部工具栏改为图标按钮。 |
| `src/extension.ts` | Webview 顶部添加和新建分组入口接入 VSCode 原生输入；添加流程复用现有主播搜索候选并补充失败提示；新建分组新增原生输入校验。 |
| `media/webview.js` | 移除自制搜索弹窗、搜索防抖、搜索结果渲染和控制面板内分组输入逻辑；顶部图标按钮只负责调用扩展宿主原生输入。 |
| `media/webview.css` | 顶部工具栏改为紧凑图标布局；删除自制搜索弹窗和搜索结果样式；删除控制面板内分组输入样式。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/product-overview.md` | 更新添加直播间、新建分组和控制面板常驻图标入口描述。 |
| `docs/prd/modules/live-monitor.md` | 更新添加候选、搜索失败、新建分组和 `RoomSearchResult` 展示位置为 VSCode 原生输入/候选列表。 |
| `docs/prd/architecture.md` | 更新 Webview 与扩展宿主消息流，明确 Webview 不再渲染添加直播间或新建分组输入弹窗。 |
| `README.md` | 补充顶部图标原生输入入口，并同步走势图时间范围描述。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，32 个测试全绿。 |
| `python .agent\\skills\\prd-keeper\\scripts\\check_prd_keeper.py --log-file docs\\prd\\prd_log\\20260813-dumpling.md` | 首次因本节验证占位失败；补全验证结果后通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\\self-work\\bwatch\\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| QuickPick 头像 | VSCode 原生候选列表不能像 Webview 自制弹窗那样直接展示主播头像；当前保留主播名、房间号、分区、粉丝、开播状态和已监控标识。 |

## B站接口代理支持与网络诊断

### 编写人

dumpling

### 请求

用户反馈当前网络环境下在线人数看起来正常，但主列表其他字段大量异常；排查发现 VSCode/Node `fetch` 在 TUN/fake-ip 代理链路中访问 B站接口可能出现 `EACCES 198.18.x`，因此要求按计划增加插件内显式代理支持和接口诊断命令。

### 影响模块

| 模块 | 影响 |
|---|---|
| 网络请求层 | 所有 B站接口请求改为经过统一 fetch 适配器，支持自动、手动和关闭代理三种模式。 |
| VSCode 配置 | 新增 BWatch 代理模式与手动代理 URL 配置项。 |
| 命令面板 | 新增网络诊断命令，输出每个 B站接口的诊断结果。 |
| 错误展示 | `fetch failed`、`EACCES 198.18.x`、TLS、DNS 等错误转换为更清晰的中文提示。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `src/network.ts` | 新增代理选择、`undici` `ProxyAgent` fetch 适配和网络错误格式化。 |
| `src/networkDiagnostics.ts` | 新增 B站基础信息、在线人数、舰队、粉丝和主播搜索接口诊断。 |
| `src/extension.ts` | 接入可重建网络适配器；监听代理配置变化；注册 `bwatch.diagnoseNetwork` 命令和 `BWatch` 输出面板。 |
| `src/bilibiliClient.ts` | 网络失败时使用统一错误格式化，主列表和搜索错误不再裸露 `fetch failed`。 |
| `src/config.ts` / `src/types.ts` | 新增代理配置读取、模式标准化和类型。 |
| `package.json` / `package-lock.json` | 新增 `bwatch.network.proxy.mode`、`bwatch.network.proxy.url`、`bwatch.diagnoseNetwork` 和 `undici@6.28.0`。 |
| `src/test/*.test.ts` | 增加代理优先级、代理关闭、手动代理、错误格式化和 client 错误提示测试。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `README.md` | 补充代理设置、诊断命令和使用边界。 |
| `docs/prd/README.md` | 更新命令入口和配置入口。 |
| `docs/prd/product-overview.md` | 补充网络诊断场景、代理模式业务规则和 TUN/fake-ip 风险。 |
| `docs/prd/modules/live-monitor.md` | 补充代理配置契约、诊断命令、错误格式化和外部服务边界。 |
| `docs/prd/architecture.md` | 补充网络适配层、诊断模块、数据流和环境变量边界。 |

### 验证

| 命令 | 结果 |
|---|---|
| `npm.cmd test` | 通过，41 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |
| `npm.cmd audit --omit=dev` | 当前网络环境下请求 npm audit 接口失败，报 `connect EACCES 198.18.0.40:443`；已改用 `undici@6.28.0`，`npm install` 本身显示 `found 0 vulnerabilities`。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 代理类型 | 第一版只支持 HTTP/HTTPS 代理，不支持 SOCKS；使用 Clash/Mihomo 时应配置 HTTP 或 mixed 端口。 |
| 代理服务可用性 | 如果代理服务或规则本身不可用，插件只能按配置走代理并提供诊断信息，不能修复外部代理链路。 |
| 网络差异 | `curl`、浏览器和 VSCode 扩展宿主可能走不同网络栈；诊断命令以扩展宿主当前 fetch 链路为准。 |

## 当前系统代理环境兼容与侧栏空白修复

### 编写人

dumpling

### 请求

用户反馈安装代理支持版本后侧栏一直加载不出来，并确认 Clash Verge 已开启系统代理和虚拟网卡；需要修复扩展激活问题，并让插件在当前代理环境中尽可能自动发现可用代理，同时给出真实网络故障原因。

### 影响模块

| 模块 | 影响 |
|---|---|
| VSIX 运行时 | 移除未被 VSIX 打包的 `undici` 运行时依赖，避免扩展激活失败导致侧栏空白。 |
| 网络请求层 | `auto` 模式在直连或显式代理失败后探测常见 Clash/Mihomo 本地 HTTP 端口，当前环境优先探测 `127.0.0.1:7897`。 |
| 网络诊断 | 输出所有自动代理候选的逐项失败原因，并修复搜索诊断请求头包含非 ASCII 字符导致的 Header ByteString 错误。 |
| PRD/README | 明确系统代理不一定被 VSCode/Node 继承，以及代理服务可用不等于 B站直播域名 TLS 链路可用。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `src/network.ts` | 改为零外部运行时依赖的代理请求实现；auto 候选依次包含 `7897`、`7890`、`10809`；支持直连/显式代理失败后的候选重试、`curl --proxy --http1.1` 兜底、代理 URL 脱敏和聚合错误信息。 |
| `src/networkDiagnostics.ts` | 诊断输出增加 auto 本地代理候选，并将主播搜索诊断 `referer` 中的中文关键词改为 URL 编码。 |
| `src/test/network.test.ts` | 增加 auto 本地代理候选顺序测试。 |
| `package.json` / `package-lock.json` | 移除 `undici` 运行时依赖，保持 VSIX 在不打包 `node_modules` 时可独立激活。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `README.md` | 补充 auto 模式会探测常见 Clash/Mihomo 本地 HTTP 端口。 |
| `docs/prd/product-overview.md` | 补充系统代理继承边界、本地端口探测和 B站直播域名 TLS 链路风险。 |
| `docs/prd/modules/live-monitor.md` | 更新代理配置契约、auto 候选和网络层零额外运行时依赖约束。 |
| `docs/prd/architecture.md` | 更新零外部运行时依赖网络层和 auto 本地代理候选数据流。 |

### 验证

| 命令/检查 | 结果 |
|---|---|
| `npm.cmd test` | 通过，42 个测试全绿。 |
| 当前系统代理检查 | Clash Verge 本地 HTTP/mixed 端口为 `127.0.0.1:7897`；Windows WinHTTP 仍显示直连，说明 VSCode/Node 不应只依赖系统代理继承。 |
| `curl.exe -x http://127.0.0.1:7897` | 百度、Google 和 `api.bilibili.com` 普通 B站接口可访问；`live.bilibili.com` 与 `api.live.bilibili.com` 在 CONNECT 成功后 TLS 握手失败。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；VSIX 包含扩展自身代码和资源，不依赖被排除的 `node_modules`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| B站直播域名链路 | 当前 `7897` 代理本身可用，但代理规则或节点对 B站直播域名的 HTTPS/TLS 链路失败；插件可以自动选择端口并清晰诊断，不能在扩展内部修复 Clash 规则或上游节点。 |
| 用户侧验证 | 需要在 Clash Verge 中让 `api.live.bilibili.com`、`live.bilibili.com` 和 `*.live.bilibili.com` 走可用节点，或临时切换全局模式/节点确认链路恢复。 |

## 主列表分组展开箭头对齐修复

### 编写人

dumpling

### 请求

用户反馈主列表分组展开时箭头与“全部”等分组标题发生垂直错位，需要修复展开和收起状态的图标对齐。

### 影响模块

| 模块 | 影响 |
|---|---|
| Webview 主列表 | 分组折叠箭头改为固定尺寸、垂直居中的 CSS 图标，不再受系统字体字形基线影响。 |
| PRD | 明确分组箭头的稳定尺寸和对齐规则。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `media/webview.js` | 移除展开/收起箭头文本字符，并将装饰性箭头标记为 `aria-hidden`。 |
| `media/webview.css` | 使用固定 `14px` 图标槽位和 CSS 边框绘制箭头；展开态向下、收起态向右，并保持垂直居中。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/modules/live-monitor.md` | 补充分组折叠箭头不依赖字体且与标题垂直居中的规则。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，42 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 视觉验证 | 自动验证覆盖脚本语法、编译、测试与打包；箭头最终观感仍以 VSCode Webview 实际渲染为准。 |

## 主列表控制项常驻布局调整

### 编写人

dumpling

### 请求

用户希望直接影响下方主列表的调整项不再放入折叠控制面板，例如视图过滤、详细/简略展示切换、列表小图开关和小图最近时间范围设置。

### 影响模块

| 模块 | 影响 |
|---|---|
| Webview 主列表控制 | 展示模式、开播状态筛选、小图开关和小图时间范围提升为常驻控制区。 |
| 折叠控制面板 | 收敛为排序、分组、刷新和聚合走势，总览与历史仍保留在面板内。 |
| 响应式布局 | 常驻控制区在宽侧栏中紧凑排列，在窄侧栏中自动换行。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `src/webviewProvider.ts` | 将主列表相关控件移出 `control-panel`；聚合走势区只保留总览和历史；保留全部原有控件 ID。 |
| `media/webview.css` | 新增常驻列表控制区和小图控制行样式，将聚合走势按钮调整为两列，并补充窄侧栏响应式规则。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/modules/live-monitor.md` | 更新常驻主列表控制与折叠面板功能边界。 |
| `docs/prd/architecture.md` | 更新 Webview 控件分层和渲染数据流描述。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，42 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 视觉验证 | 自动验证覆盖语法、编译、测试和打包；不同侧栏宽度下的最终密度仍以 VSCode Webview 实际渲染为准。 |

## 聚合走势图当前筛选范围合计曲线

### 编写人

dumpling

### 请求

用户希望总览和历史走势图在任意筛选条件下，都能把该筛选条件包含主播的在线人数求和，并作为额外数据项加入走势图；切换筛选条件时清除已添加的筛选条件合计项。

### 影响模块

| 模块 | 影响 |
|---|---|
| 总览走势图 | 任意候选范围增加“Σ 合计”开关，当前范围在线人数合计作为独立曲线、图例和悬浮读数展示。 |
| 历史走势图 | `全部`、`有数据` 和任意自定义分组均支持当前范围合计曲线。 |
| Webview 状态 | 总览和历史合计开关独立持久化；切换或重复点击候选范围时关闭对应合计并重置图例隐藏状态。 |
| 数据口径 | 合计只在 Webview 中按同一采样时间戳生成，不写回历史存储；任一候选主播在该时刻缺失或为 `null` 时合计断线。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `media/webview.js` | 新增总览/历史合计开关、范围名称、合计虚拟序列计算、状态持久化和筛选切换清理；合计序列接入统一纵轴、SVG、图例隐藏和悬浮读数。 |
| `media/webview.css` | 合计按钮使用虚线边框；合计曲线使用更粗虚线；合计图例使用加粗文字区分。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `README.md` | 补充总览和历史图当前筛选范围合计能力。 |
| `docs/prd/product-overview.md` | 补充合计曲线使用场景和筛选切换行为，并同步常驻列表控制区现状。 |
| `docs/prd/modules/live-monitor.md` | 记录合计口径、交互、状态持久化、图例和 `null` 断线规则。 |
| `docs/prd/architecture.md` | 记录 Webview 合计虚拟序列数据流和不写回历史存储的边界。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，42 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 历史完整性 | 某主播在较早时段尚未开始采样时，该时刻无法形成完整范围合计，因此按 `null` 断线处理，不把缺失历史推断为 0。 |
| 纵轴变化 | 合计值通常高于单主播值，开启后统一纵轴会扩展到合计范围，单主播曲线视觉高度会相应压缩，这是绝对值对比的预期行为。 |

## 聚合走势图图例在线人数

### 编写人

dumpling

### 请求

用户希望在总览和历史走势图下方图例的主播名字后显示具体在线人数数字。

### 影响模块

| 模块 | 影响 |
|---|---|
| 总览图例 | 主播和合计名称后显示当前图表窗口内最后一个有效采样点的在线人数。 |
| 历史图例 | 显示所选时间段内每条曲线最后一个有效采样点的在线人数。 |
| 图例隐藏 | 隐藏曲线时名称和数字一起显示删除线；没有有效采样时数字显示 `--`。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `media/webview.js` | 新增 `findLatestValidPoint()`，图例项追加格式化在线人数值。 |
| `media/webview.css` | 图例项改为颜色、名称、数值三列布局；数值使用等宽数字并支持隐藏删除线。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/product-overview.md` | 补充图例显示当前图表范围最后有效在线人数。 |
| `docs/prd/modules/live-monitor.md` | 明确总览、历史及无有效采样时的图例数字口径。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，42 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 数值时点 | 图例展示的是当前窗口内最后一个有效采样值，不是鼠标悬浮时刻值；悬浮时刻仍由图内浮层单独展示。 |

## 聚合走势图多范围合计对比

### 编写人

dumpling

### 请求

用户希望在总览和历史走势图的任意主筛选条件下，仍可分别添加其他筛选条件的合计走势，并同时比较多个自定义分组或内置范围的在线人数合计。

### 影响模块

| 模块 | 影响 |
|---|---|
| 聚合筛选控件 | 每个主筛选项旁增加独立 `Σ` 操作，主筛选与合计选择解耦。 |
| 合计序列 | 支持同时构造多个范围的合计曲线，每条曲线拥有独立 ID、名称和稳定颜色。 |
| Webview 状态 | 总览和历史分别持久化已选合计范围集合；切换主筛选时清空对应集合。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `media/webview.js` | 将单一合计布尔状态改为范围集合；按范围独立选取成员、构造合计序列，并兼容迁移旧状态。 |
| `media/webview.css` | 将筛选项排版为主筛选按钮与紧凑 `Σ` 按钮组合。 |
| `README.md` | 将当前范围单合计能力更新为多范围合计对比。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/product-overview.md` | 补充任意主筛选下可添加多个范围合计的产品规则。 |
| `docs/prd/modules/live-monitor.md` | 明确按钮、状态、序列口径及切换清理规则。 |
| `docs/prd/architecture.md` | 更新 Webview 多合计序列与状态数据流。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，42 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 颜色数量 | 合计曲线使用有限的 VSCode 图表色板；自定义分组数量超过色板容量时颜色会循环复用，但合计名称与虚线仍可区分。 |

## 主列表分组表头人数合计

### 编写人

dumpling

### 请求

用户希望固定“全部”和所有自定义分组的表头显示该组总在线人数与总舰长/舰队人数，并与简略列表中的在线人数、舰队人数列对齐。

### 影响模块

| 模块 | 影响 |
|---|---|
| 主列表分组 | 全部和自定义分组表头新增在线人数、舰队总人数合计。 |
| 响应式布局 | 表头复用详细/简略主播行对应列宽，窄屏下同步调整位置。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `media/webview.js` | 按分组当前展示房间计算两项合计，缺失值按 `0` 贡献，并保留主播数量。 |
| `media/webview.css` | 增加详细、简略和窄屏表头网格，使合计数字与对应主播指标列对齐。 |
| `README.md` | 补充分组表头汇总能力。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/product-overview.md` | 增加分组表头合计及对齐行为。 |
| `docs/prd/modules/live-monitor.md` | 明确合计范围、缺失值口径和折叠行为。 |
| `docs/prd/architecture.md` | 记录 Webview 即时聚合与 CSS Grid 对齐方式。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，42 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 合计范围 | 表头合计跟随主列表当前状态筛选；同一主播属于多个自定义分组时，会分别计入各分组，但“全部”中只计一次。 |

## 聚合走势图图例按在线人数降序

### 编写人

dumpling

### 请求

用户希望总览走势图下方图例按照在线人数从多到少排序。

### 影响模块

| 模块 | 影响 |
|---|---|
| 总览图例 | 按当前图表窗口内最后一个有效在线人数降序排列。 |
| 历史图例 | 复用相同图例函数，同步按所选时间段最后有效在线人数降序排列。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `media/webview.js` | 图例渲染前按已显示的最新有效在线人数稳定降序排序，缺失值排在末尾。 |
| `README.md` | 补充图例在线人数降序能力。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/product-overview.md` | 补充图例排序口径。 |
| `docs/prd/modules/live-monitor.md` | 明确总览与历史图例排序、同值和缺失值规则。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，42 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 动态顺序 | 刷新或改变图表时间范围后，最后有效在线人数可能变化，因此图例顺序会随之更新。 |

## 走势图展开运行时异常修复

### 编写人

dumpling

### 请求

用户反馈前两轮分组表头与图例排序修改后，小图、总览和历史走势图均无法打开。

### 影响模块

| 模块 | 影响 |
|---|---|
| SVG 绘制 | 恢复按原曲线数组绘制，移除误插入的图例排序代码。 |
| 总览/历史图例 | 在正确的图例函数中计算最新有效点并降序渲染。 |
| 主列表重绘 | 消除聚合图 `ReferenceError`，避免主渲染中断后阻止单主播小图插入。 |

### 根因

上一轮补丁错误匹配了 SVG 构造函数中的同名循环，将图例排序逻辑插入 `buildOverviewSvg()`；同时 `overviewLegend()` 直接引用未定义的 `latestPoint`。当总览或历史处于展开状态时，主 `render()` 在重建聚合图阶段抛出 `ReferenceError`，后续主播列表重建不再执行，因此小图按钮也只改变选中状态而无法插入走势图。

### 代码变更

| 文件 | 变更 |
|---|---|
| `media/webview.js` | 恢复 SVG 原始绘制循环，将排序及 `latestPoint` 解构完整移动到 `overviewLegend()`。 |
| `src/test/webviewRuntime.test.ts` | 新增真实 Webview 脚本运行时回归测试，断言排序逻辑不进入 SVG 函数，并验证图例输出 `500、200、--`。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/modules/live-monitor.md` | 明确图例排序与 SVG 绘制隔离，以及聚合图不得中断主列表重绘。 |
| `docs/prd/architecture.md` | 记录图例视图排序边界和运行时回归测试。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，43 个测试全绿，包含新增 Webview 运行时回归测试。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；包内脚本已确认排序仅位于图例函数；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 测试边界 | 新测试覆盖本次错误函数与运行时变量；完整 VSCode Extension Development Host 点击链路仍属于手工集成验证范围。 |

## 分组表头开播人数比例

### 编写人

dumpling

### 请求

用户希望主列表全部和自定义分组表头最右侧人数从单一总数改为 `开播人数/总人数`，例如 `2/10`。

### 影响模块

| 模块 | 影响 |
|---|---|
| 主列表分组表头 | 最右侧计数改为开播人数与当前分组展示总人数比例。 |

### 代码变更

| 文件 | 变更 |
|---|---|
| `media/webview.js` | 按 `status === 'live'` 统计开播人数，并渲染为 `live/total`。 |
| `README.md` | 补充分组表头开播/总数能力。 |

### PRD 更新

| 文件 | 更新 |
|---|---|
| `docs/prd/product-overview.md` | 增加开播人数/总人数展示规则。 |
| `docs/prd/modules/live-monitor.md` | 明确统计口径、位置和筛选范围。 |

### 验证

| 命令 | 结果 |
|---|---|
| `node --check media/webview.js` | 通过。 |
| `npm.cmd test` | 通过，43 个测试全绿。 |
| `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260813-dumpling.md` | 通过。 |
| `npx.cmd vsce package` | 通过，生成 `D:\self-work\bwatch\bwatch-0.1.0.vsix`；仍有既有 `repository` 字段缺失 warning。 |

### 风险/后续事项

| 项目 | 说明 |
|---|---|
| 筛选口径 | 分母沿用当前列表筛选后的分组人数；切换为“开播”筛选时可显示为 `N/N`。 |
## 聚合走势图范围多选与状态解耦

### 需求基线

- 总览和历史走势图的范围标签从单选改为多选，图表展示所有已选范围内主播的去重并集。
- 范围标签只控制主播曲线候选集合，不再清空用户手动添加的范围合计曲线。
- 图例手动隐藏状态同样不受范围标签切换影响；合计曲线只通过各范围旁的 `Σ` 按钮添加或取消。
- 默认选择 `全部`，允许用户取消至零个范围；此时仍可单独展示已加入的合计曲线。

### 实现变化

- Webview 将总览和历史的单值范围状态改为持久化集合，并兼容迁移旧版 `overviewRangeMode` / `historyRangeMode` 单值状态。
- 新增多范围成员并集逻辑，按房间号去重并保持原始房间顺序，避免重复曲线及颜色跳动。
- 范围按钮改为独立切换选中状态，不再清空合计范围集合或图例隐藏集合。
- 删除自定义分组时只裁剪对应失效范围和合计项，不强制恢复 `全部`。
- 增加运行时回归测试，覆盖多范围并集去重、状态解耦以及旧状态迁移。
## 开播提醒补充主播名

| 项目 | 说明 |
|---|---|
| 编写人 | dumpling |
| 需求 | 开播提醒除直播标题外，需要明确显示主播名字。 |
| 影响模块 | `LiveMonitor` 开播状态机、VSCode 通知层、直播监控 PRD。 |
| 代码变更 | 通知契约新增 `anchorName` 参数，通知格式改为“主播名 已开播：直播标题”，主播名缺失时回退房间号。 |
| PRD 更新 | 已同步产品概览、直播监控模块和架构数据流。 |
| 验证 | 待执行 TypeScript 编译、完整测试、PRD 校验和 VSIX 打包。 |
| 风险/后续 | B站主播名字段暂时缺失时仍会显示房间号，不影响打开直播间操作。 |
## 合计曲线显示开播人数

| 项目 | 说明 |
|---|---|
| 编写人 | dumpling |
| 需求 | 聚合走势图的合计数据需要显示对应筛选范围内的实际开播人数，格式为 `a/b`。 |
| 影响模块 | 总览走势图、历史走势图、合计图例和悬浮读数。 |
| 代码变更 | 合计虚拟序列名称新增 `当前开播人数/范围实际总人数`；人数按该合计范围自身成员计算，并同步呈现在图例与悬浮浮层。 |
| PRD 更新 | 已同步产品概览、直播监控模块和 Webview 架构说明。 |
| 验证 | 待执行 JavaScript 语法检查、完整测试、PRD 校验和 VSIX 打包。 |
| 风险/后续 | 历史走势图中的开播人数表示当前快照状态，不回溯历史时刻的直播状态。 |
