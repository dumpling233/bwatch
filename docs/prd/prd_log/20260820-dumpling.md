# 历史走势支持相邻双日查询

### 编写人

dumpling

### 请求

历史数据最多选择相邻两天，支持跨午夜直播的连续在线人数走势；双日时间范围扩展到 48 小时，横轴和提示显示日期，并标记第二天 00:00。

### 影响模块

- `src/onlineHistoryStore.ts` 历史日期范围查询。
- `src/webviewProvider.ts` 历史查询消息契约。
- `media/webview.js` 日期选择、48 小时范围条和跨日 SVG 坐标轴。
- 历史查询与 Webview 运行时测试。

### 代码变更

- `OnlineHistoryStore` 新增连续日期范围查询；保留 `queryDateHistory()` 作为单日兼容入口。
- `HistoryQueryResult` 增加 `dates` 和双日 `boundaryMs`，Webview 消息改为发送日期数组。
- 历史日期按钮支持单日/相邻双日切换，单日使用 24 小时分钟轴，双日使用 48 小时分钟轴。
- 双日历史图横轴刻度、范围标签和悬浮时间显示日期，并在第二天本地 00:00 绘制午夜分界线。

### PRD 更新

- 已同步 `docs/prd/product-overview.md`、`docs/prd/modules/live-monitor.md`、`docs/prd/architecture.md` 和 `README.md`。

# 弹幕机测试 VSIX 打包

### 编写人

dumpling

### 请求

为新增实时弹幕机功能生成一个新的 VSIX 测试安装包。

### 影响模块

- `.vscodeignore`：VSIX 生产运行时依赖收集规则。
- `.temp/bwatch-0.1.0-danmaku-test.vsix`：本地测试安装包。

### 代码变更

- 移除对全部 `node_modules` 的打包排除规则，确保弹幕客户端运行时需要的 `ws`、`https-proxy-agent` 及其生产传递依赖进入 VSIX。
- 重新生成测试包；弹幕 Webview 静态资源和编译产物均已包含。

### PRD 更新

- 已在 `docs/prd/architecture.md` 补充 VSIX 运行时依赖约束；本次不改变产品功能和用户交互。

### 验证

- `npx.cmd vsce package --out .temp\\bwatch-0.1.0-danmaku-test.vsix`：成功，71 个文件，154.41 KB。
- `npx.cmd vsce ls --tree`：确认包含 `media/danmaku.js`、`media/danmaku.css`、全部弹幕编译产物、`ws`、`https-proxy-agent` 及其生产传递依赖。
- `npm.cmd test`：85 项测试全部通过。

### 风险/后续事项

- VSCE 仍提示 `package.json` 缺少 `repository` 字段，不影响本地测试安装。
- 该包验证了编译、单元测试和打包完整性；真实 WSS 收包仍需在宿主网络中安装后复验。

# 实时弹幕机子模块

### 编写人

dumpling

### 请求

参考市面弹幕姬的实时消息获取方式，为 BWatch 增加独立的 B站直播间实时弹幕机功能。

### Baseline PRD 回填

- 代码确认：现有模块只通过 HTTP 接口轮询直播状态，没有 WebSocket、弹幕协议解析或弹幕界面。
- 文档确认：原产品边界明确把弹幕列为非目标；用户本次要求优先，实施后需要调整该边界。
- 接口验证：匿名调用 `getDanmuInfo` 不带 WBI 签名返回 `-352`，带当前 WBI 签名可取得连接 token 和弹幕服务器列表。
- 外部实现证据：主流开源实现采用真实房间号初始化、WBI 签名、WebSocket 认证、30 秒心跳、16 字节包头以及 zlib/Brotli 递归解包。

### 当前状态

- 已完成弹幕机子模块实现并把最小 Baseline 更新为最终当前行为。

### 影响模块

- `src/wbiSigner.ts`、`src/danmakuProtocol.ts`、`src/danmakuClient.ts`：签名、协议和实时会话。
- `src/danmakuWebviewProvider.ts`、`media/danmaku.js`、`media/danmaku.css`：独立实时弹幕 View。
- `src/extension.ts`、`package.json`、`package-lock.json`：扩展注册、依赖和生命周期接入。
- 弹幕协议/WBI 单元测试、手动真实链路探针与产品文档。

### 代码变更

- 新增 `bwatch.danmaku` Webview View，支持监控房间选择、其他房间号输入、连接/断开、状态、人气、清屏、自动滚动和打开直播间。
- 新增匿名 WBI 签名与 `getDanmuInfo` 初始化链路，支持 `-352` 刷新重试。
- 新增 WebSocket 认证、30 秒心跳、服务器与代理候选故障切换、1/2/5/10 秒递增重连和每 3 次重连刷新 token。
- 新增 16 字节大端包头解析、zlib/Brotli 递归解压，以及经典/新版 `DANMU_MSG` 用户字段兼容。
- 扩展宿主和 Webview 各保留最近 500 条内存弹幕；不持久化、不登录、不发送弹幕。
- 新增 `ws`、`https-proxy-agent@5` 运行时依赖和 `npm run probe:danmaku -- <room-id>` 真实链路探针。

### PRD 更新

- 已新增并完成 `docs/prd/modules/danmaku-viewer.md`，同步模块索引、PRD 总入口、产品概览、架构、项目地图和 `README.md`。
- 已把产品边界从“不提供弹幕”调整为“不提供发送弹幕；只接收单房间文本弹幕”。

### 验证

- 匿名 `getDanmuInfo`：无 WBI 签名返回 `-352`，使用当前 WBI 混排和签名后返回 `code: 0`、token 与三个弹幕服务器候选。
- `npm.cmd test`：85 项测试全部通过，其中新增 7 项 WBI、包头、zlib/Brotli、多子包和新旧弹幕字段测试。
- `node --check media/danmaku.js`：通过。
- `git diff --check`：通过，无空白错误。

### 风险/后续事项

- 真实 WSS 收包探针在沙箱内被当前 TUN/fake-ip 网络拦截；申请宿主网络权限时审批服务异常拒绝，因此本轮未完成真实弹幕收包证明。正常宿主网络可运行 `npm run probe:danmaku -- 13` 复验。
- B站网页端 WBI、弹幕服务器配置和 WebSocket 协议均为非正式接口；后续字段或鉴权变化需要继续适配。
- 当前只展示文本弹幕；礼物、上舰、醒目留言、进场、过滤、持久化和多房间聚合不在本次范围。

### 验证

- `node --check media\\webview.js`
- `npm.cmd test`：73 项测试全部通过。
- `python .agent\\skills\\prd-keeper\\scripts\\check_prd_keeper.py --log-file docs\\prd\\prd_log\\20260820-dumpling.md`
- `npx.cmd vsce package --out bwatch-0.1.0.vsix`

### 风险/后续事项

- 仅支持相邻两个自然日，不跨越三天或非连续日期；日期按钮只允许选择本地已存在历史采样的日期。

# 走势图颜色扩展

### 编写人

dumpling

### 请求

扩展历史和总览聚合图的颜色数量，尽量保证每条曲线使用不同颜色，提升多主播对比时的直观性。

### 影响模块

- `media/webview.js` 总览/历史房间曲线和合计曲线颜色分配。
- Webview 走势图回归测试与产品文档。

### 代码变更

- 使用 24 色高区分度调色板替代原有少量 VSCode 图表颜色。
- 房间曲线按稳定监控房间索引取色，筛选变化不改变主播颜色。
- 合计曲线从房间数量之后取色，避免与同图主播曲线重复。
- 曲线数量超过调色板后使用确定性 HSL 颜色，不循环复用已有颜色。

### PRD 更新

- 已同步 `docs/prd/product-overview.md`、`docs/prd/modules/live-monitor.md`、`docs/prd/architecture.md` 和 `README.md`。

# 弹幕显示控制面板

### 编写人

dumpling

### 请求

为实时弹幕机增加逐项显示控制，允许隐藏发送者、UID、粉丝牌等元数据，最终可只保留弹幕接收时间和内容；同时说明并允许隐藏弹幕左侧颜色条。

### 影响模块

- `src/danmakuWebviewProvider.ts`：显示控制面板 HTML。
- `media/danmaku.js`、`media/danmaku.css`：显示选项状态、即时重绘和界面样式。
- `src/test/danmakuWebviewRuntime.test.ts`：弹幕 Webview 显示控制回归测试。
- 实时弹幕模块 PRD、产品概览、架构和 `README.md`。

### 代码变更

- 新增可折叠“显示”控制面板，可分别控制接收时间、发送者名称、发送者 UID、粉丝牌、房管标记和弹幕颜色条。
- 弹幕正文始终显示；UID 和房管标记默认关闭，其余选项默认保持原展示效果。
- 选项变化立即重绘当前内存中的已有弹幕；控制面板展开状态和各选项通过 Webview 状态保存。
- 明确左侧颜色条来自 B站 `DANMU_MSG` 的弹幕文字颜色字段，并支持独立隐藏。

### PRD 更新

- 已同步 `docs/prd/modules/danmaku-viewer.md`、`docs/prd/product-overview.md`、`docs/prd/architecture.md` 和 `README.md`。

### 验证

- `npm.cmd test`：88 项测试全部通过，其中新增 3 项弹幕显示控制回归测试。
- `node --check media\\danmaku.js`：通过。
- `npx.cmd vsce package --out .temp\\bwatch-0.1.0-danmaku-display-controls-test.vsix`：成功，71 个文件，155.61 KB；包含弹幕资源、编译产物及 WebSocket 生产依赖。

### 风险/后续事项

- 本轮验证覆盖编译、单元测试、脚本语法和打包完整性；真实 WSS 收包及 VSCode Webview 视觉效果仍需安装测试包后在宿主环境复验。

# 最新弹幕底部状态栏

### 编写人

dumpling

### 请求

在 VSCode 窗口底部状态栏实时显示弹幕机收到的最新一条弹幕内容。

### 影响模块

- `src/danmakuStatusBar.ts`：最新弹幕状态栏控制器和文本格式化。
- `src/extension.ts`：状态栏项创建、注册与生命周期接入。
- `src/test/danmakuStatusBar.test.ts`：状态栏内容和状态流转测试。
- 实时弹幕模块 PRD、产品概览、架构、项目地图和 `README.md`。

### 代码变更

- 新增稳定 ID 为 `bwatch.latestDanmaku` 的 VSCode 左侧状态栏项，订阅现有弹幕会话事件，不新增网络连接或轮询。
- 每条新弹幕到达时立即替换状态栏文本；连续空白压缩为单个空格，最多显示 48 个 Unicode 字符，完整房间、发送者、接收时间和正文放入悬浮提示。
- 未使用弹幕功能时隐藏状态栏项；连接、等待、重连和错误状态显示对应提示；主动断开保留最后一条并切换断开图标，清屏后按当前连接状态重置或隐藏。
- 对正文中的 Codicon 起始语法进行转义，避免用户弹幕改变状态栏图标渲染。

### PRD 更新

- 已同步 `docs/prd/README.md`、`docs/prd/modules/danmaku-viewer.md`、`docs/prd/product-overview.md`、`docs/prd/architecture.md`、项目地图和 `README.md`。

### 验证

- `npm.cmd test`：90 项测试全部通过，其中新增 2 项最新弹幕状态栏测试。
- `npx.cmd vsce package --out .temp\\bwatch-0.1.0-danmaku-statusbar-test.vsix`：成功，72 个文件，157.1 KB；包含 `out/danmakuStatusBar.js` 和弹幕 WebSocket 生产依赖。

### 风险/后续事项

- 状态栏可用宽度受窗口尺寸和其他扩展状态栏项影响，因此正文固定截断并依赖悬浮提示展示完整内容；用户可以通过状态栏上下文菜单隐藏该项。
- 真实弹幕更新效果仍需安装测试包后在宿主 VSCode 和正常网络环境中复验。

# 弹幕 Emoji 显示开关

### 编写人

dumpling

### 请求

在弹幕显示控制面板增加 Emoji 表情显示开关；关闭后不直接删除表情，而是在原位置显示可读的中文名称或 Unicode 码位文本。

### 影响模块

- `src/danmakuWebviewProvider.ts`、`media/danmaku.js`：Emoji 开关、状态保存、列表文本化和宿主消息同步。
- `src/danmakuStatusBar.ts`、`src/extension.ts`：底部最新弹幕状态栏同步文本化。
- `src/test/danmakuWebviewRuntime.test.ts`、`src/test/danmakuStatusBar.test.ts`：Emoji 识别、显示恢复和状态栏回归测试。
- 实时弹幕模块 PRD、产品概览、架构和 `README.md`。

### 代码变更

- “显示”控制面板新增默认开启的“Emoji 表情”勾选项，并通过 Webview 状态保存。
- 关闭后按完整序列识别 Unicode Emoji、ZWJ 组合、肤色、旗帜和键帽，并在原位置转换为方括号文本；常见表情优先显示中文名称，例如 `👍` 转为 `[点赞]`，未知表情以 `[Emoji U+码位]` 显示。
- 纯 Emoji 弹幕转换后仍显示并计入可见条数；`[doge]` 等普通文字形式的表情代码保持不变。重新开启后从内存原始消息恢复 Emoji 图形。
- `showEmoji` 通过 Webview 消息同步给扩展宿主，底部状态栏使用同一文本化规则并继续显示纯 Emoji 最新消息。

### PRD 更新

- 已同步 `docs/prd/modules/danmaku-viewer.md`、`docs/prd/product-overview.md`、`docs/prd/architecture.md` 和 `README.md`。

### 验证

- `npm.cmd test`：92 项测试全部通过，其中新增 2 项 Emoji 文本化测试并扩展现有状态栏测试。
- `node --check media\\danmaku.js`：通过。
- `npx.cmd vsce package --out .temp\\bwatch-0.1.0-danmaku-emoji-toggle-test.vsix`：成功，72 个文件，157.94 KB；包含更新后的弹幕 Webview、状态栏编译产物和 WebSocket 生产依赖。

### 风险/后续事项

- 本次只识别 Unicode Emoji，不推断或改写任意方括号文字；常见 Emoji 名称由内置映射提供，未知 Emoji 使用 Unicode 码位兜底。如果后续需要处理 B站自定义图片表情，应基于协议中的表情元数据单独实现。
- 真实直播间中的 Emoji 组合覆盖和 UI 视觉效果仍需安装测试包后复验。

### 需求修正与重新验证

- 根据反馈将关闭 Emoji 后的行为从“删除/隐藏”修正为“原位可读文本化”，并同步弹幕列表与底部最新弹幕状态栏。
- `npm.cmd test`：92 项测试全部通过；TypeScript 编译通过。
- `node --check media\\danmaku.js`：通过。
- `python .agent\\skills\\prd-keeper\\scripts\\check_prd_keeper.py --log-file docs\\prd\\prd_log\\20260820-dumpling.md`：通过。
- `npx.cmd vsce package --out .temp\\bwatch-0.1.0-danmaku-emoji-text-test.vsix`：成功，72 个文件，158.96 KB；此前的 Emoji 删除式测试包不作为本次交付。

# 底部弹幕状态栏快捷开关

### 编写人

dumpling

### 请求

为 VSCode 底部最新弹幕状态栏增加快捷键，使用户可以快速开启或关闭该状态栏模块的显示。

### 影响模块

- `src/danmakuStatusBar.ts`：独立启用状态和隐藏/恢复渲染。
- `src/extension.ts`：持久化状态读取、切换命令注册和操作反馈。
- `package.json`：命令与默认快捷键贡献点。
- `src/test/danmakuStatusBar.test.ts`：关闭期间消息更新、恢复显示和扩展清单回归测试。
- 实时弹幕模块 PRD、产品概览、架构、PRD 入口和 `README.md`。

### 代码变更

- 新增命令 `bwatch.toggleDanmakuStatusBar`，默认快捷键为 Windows/Linux `Ctrl+Alt+D`、macOS `Cmd+Alt+D`。
- 状态栏显示开关默认开启，并通过扩展 `globalState` 的 `bwatch.danmakuStatusBar.enabled` 保存。
- 关闭后所有会话事件渲染都会保持状态栏项隐藏，但不影响弹幕长连接、消息接收、最近 500 条内存队列或 Emoji 显示选择；重新开启时立即显示当前最新弹幕。
- 每次切换后使用 VSCode 临时状态栏消息提示当前开启/关闭结果；用户仍可在快捷键设置中重新绑定该命令。

### PRD 更新

- 已同步 `docs/prd/README.md`、`docs/prd/modules/danmaku-viewer.md`、`docs/prd/product-overview.md`、`docs/prd/architecture.md` 和 `README.md`。

### 验证

- `npm.cmd test`：94 项测试全部通过；TypeScript 编译通过。
- `git diff --check`：通过，仅输出工作区既有 LF/CRLF 转换提示。
- `npx.cmd vsce package --out .temp\\bwatch-0.1.0-danmaku-statusbar-shortcut-test.vsix`：成功，72 个文件，159.31 KB；包含命令清单、状态栏编译产物和 WebSocket 生产依赖。

### 风险/后续事项

- 默认快捷键可能与用户自己的键位或其他扩展冲突；VSCode 会按其键位优先级处理，用户可在“键盘快捷方式”中搜索命令名称后重新绑定。
- 本轮使用单元测试验证状态切换与扩展清单，真实 VSCode 宿主中的按键触发和窗口重载恢复仍需安装测试包后复验。

# 实时 Super Chat 模块

### 编写人

dumpling

### 请求

确认弹幕机是否能够获取 Super Chat；若不能，则新增实时 SC 获取与展示模块。

### 影响模块

- `src/danmakuProtocol.ts`、`src/danmakuClient.ts`：SC 协议标准化、独立队列、去重和删除事件。
- `src/danmakuWebviewProvider.ts`、`media/danmaku.js`、`media/danmaku.css`：普通弹幕/SC 标签页与 SC 列表渲染。
- 弹幕协议、状态栏快照和 Webview 运行时测试。
- 实时弹幕模块 PRD、产品概览、架构、模块索引、总入口和 `README.md`。

### 代码变更

- 在现有 B站 WebSocket 长连接上解析 `SUPER_CHAT_MESSAGE` 和 `SUPER_CHAT_MESSAGE_JPN`，不增加轮询或第二条网络连接。
- SC 标准化金额、持续时间、发送者、UID、粉丝牌、房管标记、正文和主题色；`SUPER_CHAT_MESSAGE_DELETE` 会移除对应条目。
- 扩展宿主和 Webview 分别保留最近 100 条 SC，按 B站消息 ID 去重；切换房间和清屏会同时清空普通弹幕与 SC。
- Webview 新增“弹幕 / SC”标签页，保存当前标签；SC 使用独立列表，复用元数据与 Emoji 显示控制，但不会覆盖 VSCode 底部最新普通弹幕状态栏。

### PRD 更新

- 已同步 `docs/prd/README.md`、`docs/prd/modules/README.md`、`docs/prd/modules/danmaku-viewer.md`、`docs/prd/product-overview.md`、`docs/prd/architecture.md` 和 `README.md`。
- 当前范围明确为只接收连接建立后的实时 SC，不额外调用 REST 接口补拉正在展示或历史 SC。

### 验证

- `npm.cmd test`：99 项测试全部通过；TypeScript 编译通过。
- `node --check media\\danmaku.js`：通过。
- `python .agent\\skills\\prd-keeper\\scripts\\check_prd_keeper.py --log-file docs\\prd\\prd_log\\20260820-dumpling.md`：通过。
- `git diff --check`：通过，仅输出工作区既有 LF/CRLF 转换提示。
- `npx.cmd vsce package --out .temp\\bwatch-0.1.0-danmaku-super-chat-test.vsix`：成功，72 个文件，161.61 KB；包含更新后的弹幕资源、SC 编译产物及 WebSocket 生产依赖。
- 新增标准 SC、JPN 兼容、删除事件以及标签页/增量消息/安全渲染回归覆盖。

### 风险/后续事项

- B站直播 WebSocket 和 SC 字段属于非正式接口，字段变体仍可能需要后续兼容。
- 真实直播间 SC 收包、JPN 变体和删除事件需要在实际出现 SC 的直播间安装测试包后复验。

# 弹幕完整性与及时性修复

### 编写人

dumpling

### 请求

修复弹幕偶发卡住、延迟批量显示以及可能获取不完整的问题，提供完整、及时的实时弹幕体验。

### 影响模块

- `src/danmakuClient.ts`：逐命令容错和传输层假活检测。
- `src/danmakuWebviewProvider.ts`、`media/danmaku.js`：轻量状态快照、宿主微任务批量发送和 Webview 批量 DOM 提交。
- `src/test/danmakuClient.test.ts`、`src/test/danmakuWebviewRuntime.test.ts`：丢包、假活和批量渲染回归测试。
- 实时弹幕模块 PRD、产品概览、架构和 `README.md`。

### 代码变更

- 将 WebSocket 帧结构解包与单个业务命令解析分离；每个命令使用独立容错边界，损坏 JSON 不再丢弃同一帧后续正常弹幕。
- 认证后每 10 秒发送 WebSocket Ping；收到任意数据或 Pong 都刷新存活状态，连续一个检测周期无响应时终止旧连接并触发现有递增重连。
- 心跳和连接状态变化只向 Webview 发送轻量快照，首次加载仍发送包含最近 500 条弹幕和 100 条 SC 的完整快照。
- 同一扩展任务内的普通弹幕通过微任务合并为单个 `messageBatch`，Webview 使用 `DocumentFragment` 一次追加并只执行一次计数与自动滚动更新；底部最新弹幕状态栏仍即时更新。

### PRD 更新

- 已同步 `docs/prd/modules/danmaku-viewer.md`、`docs/prd/product-overview.md`、`docs/prd/architecture.md` 和 `README.md`。

### 验证

- `npm.cmd test`：102 项测试全部通过；TypeScript 编译通过。
- `node --check media\\danmaku.js`：通过。
- `python .agent\\skills\\prd-keeper\\scripts\\check_prd_keeper.py --log-file docs\\prd\\prd_log\\20260820-dumpling.md`：通过。
- `git diff --check`：通过，仅输出工作区既有 LF/CRLF 转换提示。
- `npx.cmd vsce package --out .temp\\bwatch-0.1.0-danmaku-realtime-fix-test.vsix`：成功，72 个文件，162.25 KB；包含更新后的弹幕会话、Webview 批量渲染资源和 WebSocket 生产依赖。

### 风险/后续事项

- 真实直播间到达间隔仍受 B站服务器和本地代理链路影响；代码已消除已确认的本地丢包、假活和重复全量重绘问题。

# 直播列表快捷打开弹幕机

### 编写人

dumpling

### 请求

弹幕机监控房间候选显示绿/红开播状态并把开播房间排列在前；直播监控列表为每个主播增加“弹”按钮，点击后切换到弹幕机并自动连接对应房间。

### 影响模块

- `src/danmakuWebviewProvider.ts`、`media/danmaku.js`：候选排序、状态标识和外部选房。
- `src/webviewProvider.ts`、`src/extension.ts`：直播列表到弹幕 View 的宿主动作桥。
- `media/webview.js`、`media/webview.css`：详细/简略主播行的“弹”按钮及对齐布局。
- 弹幕与直播监控 Webview 运行时测试、模块 PRD、产品概览、架构和 `README.md`。

### 代码变更

- 弹幕候选按开播状态稳定排序，同状态保留监控列表原顺序；原生下拉框使用绿色/红色圆点区分开播与未开播/未知。
- `DanmakuWebviewProvider` 增加受校验的外部 `connectRoom()` 入口；View 尚未创建时暂存选择，创建后再同步选中房间。
- 直播列表详细模式和简略模式均增加“弹”按钮；点击后发送 `openDanmaku`，宿主复用现有单房间会话连接并执行 `bwatch.danmaku.focus`。
- 外部进入弹幕机时同步选中目标候选、清空其他房间输入并切换到普通弹幕标签。

### PRD 更新

- 已同步 `docs/prd/modules/live-monitor.md`、`docs/prd/modules/danmaku-viewer.md`、`docs/prd/product-overview.md`、`docs/prd/architecture.md` 和 `README.md`。

### 验证

- `npm.cmd test`：105 项测试全部通过；TypeScript 编译通过。
- `node --check media\\danmaku.js`、`node --check media\\webview.js`：通过。
- `npx.cmd vsce package --out .temp\\bwatch-0.1.0-danmaku-room-shortcut-test.vsix`：成功，72 个文件，162.76 KB；包含两个 Webview 资源、跨 View 宿主逻辑和弹幕运行时依赖。

### 风险/后续事项

- 绿/红状态点使用原生下拉框中的系统彩色圆点字符，保留原生键盘操作；具体字形尺寸由 VSCode 所在系统字体决定。
- `bwatch.danmaku.focus` 是 VSCode 为已贡献 View 提供的标准聚焦命令；真实窗口中的跨 View 切换和自动连接效果仍需安装测试包复验。

# 弹幕传输与业务流健康修复

### 编写人

dumpling

### 请求

根据弹幕偶发卡住、过一段时间批量到达的分析结果，修复可能由代码判活模型、节点或本地代理线路造成的获取不及时与局部丢包问题。

### 影响模块

- `src/danmakuProtocol.ts`、`src/danmakuClient.ts`：原始发送时间、局部解包容错、双层健康检测、线路轮换和诊断。
- `src/extension.ts`：弹幕诊断接入 `BWatch` 输出面板。
- `src/test/danmakuProtocol.test.ts`、`src/test/danmakuClient.test.ts`：坏压缩包、发送时间、业务延迟、线路轮换和事件循环漂移回归测试。
- 实时弹幕模块 PRD、产品概览、架构和 `README.md`。

### 代码变更

- 将 `host_list × 代理候选` 展开为循环线路集合，成功建立某条线路后把游标推进到下一组合；异常关闭或业务退化重连不再从第一条线路重新开始。
- `DANMU_MSG` 保存 B站原始发送时间 `sentAt` 和本地收包时间 `receivedAt`。连续两条延迟超过 8 秒，或同一帧发送时间跨度超过 5 秒时，即使 Pong 与应用心跳正常也判定业务流退化并轮换线路。
- 认证成功不立即清零连续重连计数；连接稳定 60 秒或连续 5 条低延迟弹幕后才恢复健康计数，保证反复认证成功后卡顿仍能触发每 3 次刷新节点配置与 token。
- 新增安全解码结果，保留有效包与局部错误；单个 zlib/Brotli 子包解压失败只丢弃自身，损坏包长会扫描后续合法包头恢复同步。
- 记录 Pong、应用心跳、帧、普通弹幕和 Ping RTT；传输探针定时器漂移超过 5 秒时记录 Extension Host 阻塞、跳过本轮网络超时判断，并在随后 20 秒内抑制宿主阻塞造成的业务延迟误判。线路、健康状态和局部解析错误写入 `BWatch` 输出面板，不记录弹幕正文。

### PRD 更新

- 已同步 `docs/prd/modules/danmaku-viewer.md`、`docs/prd/product-overview.md`、`docs/prd/architecture.md` 和 `README.md`。

### 验证

- `npm.cmd test`：110 项测试全部通过；TypeScript 编译通过。
- `node --check media\danmaku.js`、`node --check media\webview.js`：通过。
- `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260820-dumpling.md`：通过。
- `git diff --check`：通过，仅输出工作区既有 LF/CRLF 转换提示。
- `npx.cmd vsce package --out .temp\bwatch-0.1.0-danmaku-business-health-fix-test.vsix`：成功，72 个文件，165.44 KB；包含业务健康检测、线路轮换、局部解包容错和诊断输出编译产物。

### 风险/后续事项

- B站没有可靠的匿名断线历史弹幕补拉接口；本轮通过及时识别退化和轮换线路缩短异常窗口，但无法补回断线期间服务器未再次下发的消息。
- 真实直播间收包仍受宿主网络、TUN/fake-ip 和 B站节点影响，需要安装测试 VSIX 后在正常网络环境复验。

# 弹幕无 Pong 误断修复

### 编写人

dumpling

### 请求

根据真实直播间日志修复弹幕机约每 30 秒错误断线的问题，尽量保持实时获取并减少重连窗口内的弹幕遗漏。

### 影响模块

- `src/danmakuClient.ts`：传输层判活时钟、Pong 诊断和超时条件。
- `src/test/danmakuClient.test.ts`：无 Pong 但应用心跳正常、完全静默超时的回归测试。
- 实时弹幕模块 PRD、产品概览、架构、项目坑点和 `README.md`。

### 代码变更

- 删除单个 10 秒周期的布尔存活判定；Ping 仍每 10 秒发送，但连续缺少 Pong 只累计诊断次数，不再单独触发断线。
- 使用认证时间、最后业务帧和最后 Pong 的最大值计算绝对入站静默时长；只有持续 45 秒没有任何入站活动才终止连接并轮换线路。
- 诊断新增 `missedPongs`、`inactivity` 和最后应用心跳发送时间，用于区分“不返回 Pong”与“所有业务数据真正中断”。
- 保留 30 秒应用心跳、业务消息延迟检测、线路轮换、事件循环漂移保护和局部解包容错。

### PRD 更新

- 已同步 `docs/prd/modules/danmaku-viewer.md`、`docs/prd/product-overview.md`、`docs/prd/architecture.md`、`.agent/skills/prd-keeper/references/gotchas.md` 和 `README.md`。

### 验证

- `npm.cmd test`：110 项测试全部通过；TypeScript 编译通过。
- `node --check media\danmaku.js`、`node --check media\webview.js`：通过。
- `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260820-dumpling.md`：通过。
- `git diff --check`：通过，仅输出工作区既有 LF/CRLF 转换提示。
- `npx.cmd vsce package --out .temp\bwatch-0.1.0-danmaku-no-pong-timeout-fix-test.vsix`：成功，72 个文件，165.76 KB；包含绝对静默判活、无 Pong 兼容和增强诊断编译产物。

### 风险/后续事项

- 45 秒窗口覆盖一个完整 30 秒应用心跳周期并保留 15 秒抖动余量；真正断线的发现时间会比旧版最长增加约 35 秒，以换取低弹幕房间和无 Pong 线路不被反复误断。
- WebSocket 无法补回真实断线期间 B站未重新下发的消息；本次修复通过避免无意义重连减少该窗口，但不能提供绝对零丢失保证。

# 弹幕机同步直播间监控指标

### 编写人

dumpling

### 请求

把在线人数模块中的直播间相关信息同步显示到弹幕机页面，包括在线人数、开播时长、舰队人数及其他已有房间信息，方便在查看弹幕时同时查看直播状态。

### 影响模块

- `src/danmakuWebviewProvider.ts`：完整监控房间指标映射和信息栏结构。
- `media/danmaku.js`、`media/danmaku.css`：当前房间匹配、指标渲染、本地时长更新和响应式样式。
- `src/test/danmakuWebviewRuntime.test.ts`：指标同步、无额外请求、降级状态和时长更新回归测试。
- 实时弹幕模块 PRD、产品概览、架构和 `README.md`。

### 代码变更

- 弹幕 View 连接状态下方新增紧凑房间信息栏，显示主播、标题、开播状态、在线人数、`HH:MM:SS` 开播时长、舰队总人数和粉丝数。
- `DanmakuWebviewProvider` 直接映射 `LiveRoomStatus` 现有字段并通过原有 `rooms` 消息同步，不新增轮询或 B站接口请求。
- 直播中的开播时长由 Webview 每秒只更新时长文本，不重绘弹幕列表；监控快照更新时立即刷新其余指标。
- 在线、舰队和粉丝沿用缓存时显示黄色及最近成功时间提示；手输非监控房间显示明确降级信息和 `-` 指标。
- 信息栏在窄侧栏下从四列切换为两列，主播名和标题使用截断与悬浮完整文本，避免内容溢出。

### PRD 更新

- 已同步 `docs/prd/modules/danmaku-viewer.md`、`docs/prd/product-overview.md`、`docs/prd/architecture.md` 和 `README.md`。

### 验证

- `npm.cmd test`：113 项测试全部通过；TypeScript 编译通过。
- `node --check media\danmaku.js`、`node --check media\webview.js`：通过。
- `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260820-dumpling.md`：通过。
- `git diff --check`：通过，仅输出工作区既有 LF/CRLF 转换提示。
- `npx.cmd vsce package --out .temp\bwatch-0.1.0-danmaku-room-metrics-test.vsix`：成功，72 个文件，167.56 KB；包含更新后的弹幕信息栏资源、编译产物和 WebSocket 生产依赖。

### 风险/后续事项

- 信息栏指标刷新频率与直播监控模块一致；开播时长本地每秒走秒，但开播状态、在线人数、舰队和粉丝不会绕过已有刷新周期。
- 直接输入未加入监控列表的房间不会额外拉取指标；连接状态中的 B站“人气”仍来自弹幕心跳，与在线观众人数是不同口径。

# 弹幕中断分层诊断增强

### 编写人

dumpling

### 请求

继续分析弹幕机仍存在的弹幕中断现象；当前证据不足以定责时，把后续判断需要的数据输出到日志，供用户复现后回传并继续修复。

### 影响模块

- `src/danmakuClient.ts`：WebSocket、协议解析和业务命令聚合诊断。
- `src/danmakuWebviewProvider.ts`、`src/extension.ts`：批次标识、投递结果和 Webview 诊断接入输出面板。
- `media/danmaku.js`：交付延迟、DOM 提交耗时和定时器漂移聚合回传。
- `src/test/danmakuClient.test.ts`、`src/test/danmakuWebviewRuntime.test.ts`：诊断字段、隐私边界和跨层关联回归测试。
- 实时弹幕模块 PRD、产品概览、架构和 `README.md`。

### 代码变更

- 连接期间每 10 秒输出一个流量窗口，记录帧、字节、解包、业务包、弹幕、SC、其他命令、错误、命令类型分布、发送时间跨度和收包延迟范围。
- 普通弹幕批次增加递增 `batchId` 和宿主发出时间；`postMessage` 返回 `false` 或 Promise 异常时立即写入失败原因。
- Webview 每 10 秒回传批次数、消息数、最大交付延迟、最大 DOM 提交耗时、定时器漂移、列表数量和最后批次号，仅在连接中、已连接或重连中启用。
- 所有新增诊断不记录弹幕正文、发送者、UID、粉丝牌或 SC 内容。
- 当前没有仅凭弹幕空档自动重连：低活跃直播间的自然静默无法在本地与服务端漏发可靠区分，盲目重连反而会制造断线丢失窗口。

### PRD 更新

- 已同步 `docs/prd/modules/danmaku-viewer.md`、`docs/prd/product-overview.md`、`docs/prd/architecture.md` 和 `README.md`。

### 验证

- `npm.cmd test`：115 项测试全部通过；TypeScript 编译通过。
- `node --check media\danmaku.js`、`node --check media\webview.js`：通过。
- `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260820-dumpling.md`：通过。
- `git diff --check`：通过，仅输出工作区既有 LF/CRLF 转换提示。
- `npx.cmd vsce package --out .temp\bwatch-0.1.0-danmaku-layer-diagnostics-test.vsix`：成功，72 个文件，169.37 KB；包含分层诊断、批次投递结果、Webview 聚合回传和弹幕运行时依赖。

### 风险/后续事项

- 单份插件日志无法证明直播网页在同一时间是否实际收到弹幕；复现时仍需记录网页可见弹幕时间或截图，用于与插件窗口数据对照。
- 新日志可以定位中断发生在哪一层，但最终修复策略需依据真实复现窗口决定，避免对自然静默或服务端分发差异做错误重连。

# 文本弹幕选择性饥饿修复

### 编写人

dumpling

### 请求

根据分层诊断结果修复弹幕连接仍持续收到其他业务命令、但 `DANMU_MSG` 文本弹幕长时间停止下发造成的卡住问题，尽量提高实时性并减少漏弹幕窗口。

### 影响模块

- `src/danmakuClient.ts`：文本弹幕选择性饥饿检测、线路轮换冷却和诊断日志。
- `src/test/danmakuClient.test.ts`：饥饿阈值、自然静默、基线、恢复、宿主阻塞和冷却回归测试。
- 实时弹幕模块 PRD、产品概览、架构、项目坑点和 `README.md`。

### 代码变更

- 在现有 10 秒流量窗口上增加选择性饥饿证据：当前连接必须先收到过普通弹幕，随后连续 3 个窗口仍有消息包和其他业务命令但普通弹幕为 0，距最后弹幕至少 30 秒且累计其他命令至少 12 条时，终止当前线路并沿用现有服务器/代理线路轮换。
- 收到普通弹幕、候选窗口中断或建立新连接时清零连续窗口和其他命令累计；主动重新选房时同时清除上一次饥饿轮换时间。
- 选择性饥饿轮换使用 60 秒冷却；近期发生 Extension Host 事件循环阻塞时清除可疑证据并在 20 秒保护期内不触发，减少低活跃房间和本地阻塞造成的误判。
- 输出连续无弹幕窗口、累计其他命令、距最后弹幕时间、冷却状态和当前线路，不记录弹幕正文、用户名或 UID。

### PRD 更新

- 已同步 `docs/prd/modules/danmaku-viewer.md`、`docs/prd/product-overview.md`、`docs/prd/architecture.md`、`.agent/skills/prd-keeper/references/gotchas.md` 和 `README.md`。
- 项目坑点补充“传输层活跃不等于 `DANMU_MSG` 文本弹幕流健康”，后续判定必须同时具备业务证据和误判保护。

### 验证

- `npm.cmd test`：121 项测试全部通过；TypeScript 编译通过。
- `node --check media\danmaku.js`、`node --check media\webview.js`：通过。
- `git diff --check`：通过，仅输出工作区既有 LF/CRLF 转换提示。
- `npx.cmd vsce package --out .temp\bwatch-0.1.0-danmaku-starvation-fix-test.vsix`：成功，72 个文件，170.14 KB；包含选择性饥饿检测、线路轮换保护、诊断和回归测试对应编译产物。

### 风险/后续事项

- 该规则使用保守阈值降低自然静默误判，但无法从匿名协议绝对证明 B站节点漏发；极低弹幕但其他互动非常密集的直播间仍存在误轮换可能，真实使用日志可用于继续校准阈值。
- B站没有可靠的匿名历史弹幕补拉接口；线路轮换只能缩短异常持续时间，无法补回切换前服务端未重新下发的弹幕。

# 参考开源实现重写弹幕连接层

### 编写人

dumpling

### 请求

把高星 B站弹幕项目拉取到本地，参考其连接、心跳、节点轮换和重连方式重新实现 BWatch 弹幕机的实时获取层；允许整体重写，不局限于继续修补旧逻辑。

### 本地参考基线

- `xfgryujk/blivedm@3bf17fe862b8c2fc6bc04c81c3f0c5db95de93d6`（MIT）：单一网络循环、host 轮换、完整轮换后刷新房间配置/token。
- `simon300000/bilibili-live-ws@39bd334bc03c8f340ed158bb1e6118b4f23fca41`（MIT）：心跳超时关闭和保活层重建连接。
- `copyliu/bililive_dm@38172aa0b859b8b9deb77ca2aa8d52329a17d10d`（WTFPL v2）：独立接收循环、30 秒心跳和断开事件。
- `Discreater/Bilibili-Live-Danmaku-vscode@d1aadd663a822d8a9ad413e75680995af6f3a3e9`（MIT）：VSCode 扩展会话启停和 UI 事件边界。
- 参考仓库位于 `.temp/reference-danmaku/`，只借鉴算法和状态机思路；未复制源码、未引入 Python/C# 运行时，也未进入最终 VSIX。

### 影响模块

- `src/danmakuClient.ts`：连接生命周期、单线路尝试、节点/代理轮换、配置刷新和旧连接隔离。
- `src/network.ts`：自动网络失败诊断补充直连 curl 兜底的最终错误。
- `src/test/danmakuClient.test.ts`、`src/test/danmakuLiveProbe.ts`：连接状态机回归和脱敏真实链路探针。
- `.gitignore`、`.vscodeignore`：避免提交并排除打包全部 `.temp` 参考仓库和测试产物。
- 实时弹幕模块 PRD、产品概览、架构和 `README.md`。

### 代码变更

- 重连从“一次串行尝试全部服务器 × 代理线路”改为“一次只尝试一条线路”；握手前推进轮换游标，失败后按 1/2/5/10 秒上限退避，并在同一代理候选内优先轮换 B站服务器，避免多个 12 秒握手超时连续累加。
- 会话级 `AbortController` 同时覆盖房间初始化 HTTP 请求与 WebSocket 握手；切房、主动断开和扩展停用立即取消旧任务。
- 每次连接尝试分配独立世代号；消息、错误和关闭回调同时检查当前 socket 身份，旧连接迟到事件不能清除新连接定时器、写入新房间消息或安排额外重连。
- 首次初始化的临时网络错误进入自动重试，B站明确返回 `-400` 的无效房间号才停止；认证失败强制刷新服务器配置/token，其他连续失败在至少 3 次且完整尝试一轮当前线路后刷新。
- 保留现有 WBI、zlib/Brotli 可恢复解包、文本弹幕、SC、无 Pong 兼容、业务延迟检测、选择性饥饿检测、Extension Host 阻塞保护和隐私安全诊断。
- 真实链路探针启用同一套脱敏连接日志并在完成后主动退出，便于区分 HTTP、WBI、代理和 WSS 阶段。

### PRD 更新

- 已同步 `docs/prd/modules/danmaku-viewer.md`、`docs/prd/product-overview.md`、`docs/prd/architecture.md` 和 `README.md`。
- 模块 PRD 新增参考仓库固定 commit、许可证、借鉴边界和重写后的连接状态机规则。

### 验证

- `npm.cmd test`：128 项测试全部通过；新增单线路失败轮换、服务器优先、握手取消、切房取消初始化、临时初始化重试、无效房间停止和旧 socket 迟到关闭隔离测试。
- `python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260820-dumpling.md`：通过。
- `git diff --check`：通过，仅输出工作区既有 LF/CRLF 转换提示。
- 系统 `curl` 请求房间 `13` 基础信息接口返回 HTTP 200；Codex 沙箱中的 Node/fake-ip 链路返回 `EACCES 198.18.0.52:443`，因此沙箱内无法完成真实 WSS 收包证明，诊断已明确定位为本地执行环境边界。
- `npx.cmd vsce package --out .temp\bwatch-0.1.0-danmaku-connection-rewrite-test.vsix`：成功，72 个文件，170.72 KB；包含重写后的弹幕客户端、网络诊断、Webview 资源及 `ws`/`https-proxy-agent` 生产依赖，不包含 `.temp` 和参考仓库。

### 风险/后续事项

- B站匿名 WebSocket 没有断线历史弹幕补拉能力；新状态机显著缩短已知线路失败和竞态造成的中断，但无法对服务端未重新下发的弹幕承诺绝对零丢失。
- 最终实时效果仍需在用户正常 VSCode 宿主网络中安装新 VSIX 验证；若仍出现空档，新的 `attemptGeneration`、单线路索引、总重试次数和完整网络兜底错误可用于继续精确定位。
